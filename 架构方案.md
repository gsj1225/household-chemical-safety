# 排雷挑战——架构方案

> 本文档是开发过程中的唯一技术权威参考。所有代码变更必须先更新本文档，再写代码。
> 最后更新：2026-07-18

### 2026-07-11 架构审查优化约束

- 移动端 API 地址通过 `EXPO_PUBLIC_API_BASE_URL` 注入，禁止在源码中写死开发机地址。
- 所有移动端请求统一经过带超时、错误归一化的 HTTP 客户端，避免弱网下永久等待。
- 后端上传入口统一校验 MIME 类型和文件大小，默认只接受 JPEG/PNG/WEBP，最大 5MB。
- 挑战状态使用强类型模型保存，并设置容量和过期清理；Demo 阶段仍为单进程内存实现，生产环境必须替换为 Redis/数据库。
- Mock 产品轮换进度归属于 challenge，而不是全局 AI 单例，确保并发挑战互不干扰。
- 已完成挑战禁止继续写入扫描结果；旁白、扫描、报告接口均校验 challengeId。
- 自动化测试使用 pytest + FastAPI TestClient，不依赖预先启动的服务器或临时图片文件。

### 2026-07-12 电脑 Web 预览方案

- Expo Web 作为 UI 验收与产品演示入口，补齐 `react-dom`、`react-native-web` 依赖。
- 浏览器仍可使用摄像头；同时只在 Web 平台提供“使用演示照片”入口，方便没有摄像头时走通 Mock 全流程。
- Web 上传使用标准 `Blob`，原生端继续使用 React Native FormData 文件对象。
- 本地预览默认由浏览器访问 `http://localhost:8081`，后端运行在 `http://127.0.0.1:8000`。

### 2026-07-12 Service 层与 SQLite 持久化

- API Route 只负责 HTTP 参数、依赖注入和响应模型；挑战流程编排全部迁移到 Service。
- `ChallengeRepository` 负责 SQLite 读写，Service 不直接拼写 SQL，Route 不访问数据库。
- Demo 数据库使用 SQLite，默认路径 `backend/data/challenges.db`，通过 `DATABASE_PATH` 覆盖。
- 挑战状态以 Pydantic 模型序列化保存；每次扫描和完成报告后立即提交，后端重启可恢复。
- Repository 提供过期清理、容量限制和最近挑战查询；生产环境可按同一接口替换 PostgreSQL/Redis。
- 新增 `GET /api/challenge/history`，当前只用于本地 Demo 查看最近报告，不包含用户账号隔离；正式上线前必须增加身份体系。

### 2026-07-12 历史挑战与报告恢复

- 导航新增 `History` 页面：首页进入历史列表，历史列表返回首页或打开已完成报告。
- `GET /api/challenge/history` 返回最近挑战摘要；仅已完成且含报告的记录允许进入报告详情。
- 历史报告复用现有 `POST /api/challenge/result` 的幂等读取能力，不新增重复报告模型。
- 恢复历史报告时，移动端先写入 Zustand 的 `report`，再导航到 `Result`；“再排一次”清空当前状态并回首页。
- 历史页面必须覆盖加载、空数据、网络失败、重试四种状态。
- Web 内容区统一限制最大宽度 720px 并居中，手机端保持全宽，避免桌面浏览器内容过度拉伸。

### 2026-07-12 扫描流程交互可靠性

- 相机页必须提供取消入口；拍照失败保留在相机页并给出用户可见提示，不能只写控制台。
- 全景分析返回空区域时回到全景状态，提示用户更换角度重拍，不进入无内容的引导页。
- `analyzing`、`finishing` 状态期间禁止重复拍摄、跳过或重复请求报告。
- 最后一个区域点击继续后进入明确的“生成报告中”界面；失败后回到单次结果页，可再次提交。
- 扫描页所有非相机视图采用最大 720px 居中内容区；相机仍保持全屏，兼容手机取景。
- 页面离开扫描流程时保留后端挑战记录，但本地新挑战开始会清空旧的临时 UI 状态。

### 2026-07-12 可观测性与统一错误契约

- 每个 HTTP 请求生成 `request_id`，通过响应头 `X-Request-ID` 返回；日志与错误响应使用同一 ID。
- 访问日志记录 `request_id`、method、path、status、duration_ms；禁止记录图片、API Key、请求体和完整查询参数。
- API 错误统一返回 `{"error":{"code","message","request_id"}}`，前端不依赖 FastAPI 默认 `detail`。
- 业务异常、HTTP 异常、参数校验异常和未知异常分别映射稳定错误码；未知异常只记录服务端堆栈，对客户端返回通用文案。
- 客户端 `ApiError` 保存 `requestId`，用户可见错误追加简短追踪号，便于根据日志定位。
- 日志等级通过 `LOG_LEVEL` 配置；Demo 输出到标准输出，由 Docker/部署平台负责采集与轮转。

### 2026-07-12 风险证据与内容治理

- 风险事实与展示文案分离：`risk_evidence.json` 以风险/产品 ID 关联来源，原规则文件只保存触发条件和操作建议。
- 证据状态分为 `verified`、`needs_review`；证据等级分为 `authoritative`、`secondary`、`unverified`。
- `verified` 至少需要一个可访问的权威机构来源、标题、URL 和复核日期；没有可靠来源的内容必须标记 `needs_review`，不得伪造引用。
- 风险 API 返回 `evidence_status`、`evidence_level`、`sources`；报告页明确显示“权威资料支持”或“资料待复核”。
- 删除不可追溯的具体事故、绝对化抽检比例和未经证实的因果描述；建议使用“可能、应按标签、需要检测确认”等边界清晰的措辞。
- 识别到某品类不等于检出某有害成分。品类通用提示属于预防性建议，不得呈现为产品已超标。
- 自动化测试保证每个规则和已知产品条目都有证据登记，且 `verified` 条目的来源字段完整。

### 2026-07-12 图片隐私与资源滥用防护

- 上传图片只在当前请求内以内存字节处理；不写入 SQLite、文件系统或日志，响应结束后不保留原图。
- 图片继续执行 MIME、空文件和 5MB 上限校验；AI Provider 只能接收字节，不接触 UploadFile 或客户端文件名。
- Demo 使用进程内滑动窗口限流：开始挑战 10次/分钟、扫描 30次/分钟、旁白 60次/分钟；生产多实例必须迁移到 API Gateway/Redis。
- 每个 challenge 最多允许 20 次细拍，避免更换 IP 绕过限流后持续消耗 AI 配额。
- 429 错误区分 `RATE_LIMITED` 与 `SCAN_LIMIT_REACHED`，返回 `Retry-After`（适用时）和 requestId。
- API 响应增加 `X-Content-Type-Options: nosniff`、`Cache-Control: no-store`、`Referrer-Policy: no-referrer`；HSTS 仅在 HTTPS 生产入口配置，不在本地 HTTP 启用。
- `DEBUG=false` 时禁止 `CORS_ORIGINS=["*"]`，生产必须显式列出可信 Web 域名；移动原生 HTTP 请求不依赖浏览器 CORS。
- 拍照页明确告知“照片仅用于本次分析，当前版本不保存原图”；真实第三方 AI 接入前必须更新隐私政策和数据处理说明。

---

## 一、项目概述

### 产品名称
家庭化学品排雷大挑战

### 一句话定位
拍照扫描家里的化学品，发现隐藏的安全"雷点"，生成排雷评分，发起全民挑战。

### 宣传口号
"我家到底有多少雷？"

### 技术选型总览

| 层 | 选型 | 理由 |
|---|---|---|
| 移动端 | React Native + Expo（TypeScript） | 跨平台AI辅助生成质量最高，Expo简化构建部署 |
| 状态管理 | Zustand | 比Redux简单，AI生成质量高 |
| 导航 | React Navigation v7 | Expo官方推荐，AI生成质量高 |
| 图片处理 | expo-image-manipulator | 拍照后压缩到1MB以下再上传 |
| 后端 | Python FastAPI | 与AI模型调用无缝衔接，开发效率高 |
| 数据库 | SQLite（开发）/ PostgreSQL（生产可选） | 轻量启动，无需额外服务 |
| AI接口 | 抽象接口层 + Mock实现 | 当前用Mock，后续接Qwen云API |
| 通信 | HTTP REST + JSON | 简单可靠，移动端友好 |
| 图片传输 | Base64 / Multipart | 手机拍照上传后端处理 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────┐
│                  移动端 (React Native)        │
│                                               │
│  ┌─────────┐ ┌──────────┐ ┌───────────────┐ │
│  │ 首页     │ │ 排雷页面  │ │ 结果页面       │ │
│  │ HomeScreen│ │ ScanScreen│ │ ResultScreen  │ │
│  └─────────┘ └──────────┘ └───────────────┘ │
│        │           │              │           │
│        └───────────┴──────────────┘           │
│                    │                          │
│            ┌───────┴───────┐                  │
│            │  API Client    │                  │
│            │  (api.ts)      │                  │
│            └───────┬───────┘                  │
└────────────────────┼──────────────────────────┘
                     │ HTTP REST + JSON
                     ▼
┌─────────────────────────────────────────────┐
│                  后端 (FastAPI)               │
│                                               │
│  ┌─────────────────────────────────────┐     │
│  │            API Routes                │     │
│  │  /scan/panorama /scan/identify      │     │
│  │  /scan/confirm   /challenge/result  │     │
│  │  /challenge/start /challenge/result │     │
│  └──────────────┬──────────────────────┘     │
│                 │                             │
│  ┌──────────────┴──────────────────────┐     │
│  │           Service Layer              │     │
│  │  ScanService  RiskService            │     │
│  │  ReportService  NarrationService     │     │
│  └──────┬───────────────┬───────────────┘     │
│         │               │                     │
│  ┌──────┴──────┐ ┌─────┴──────────────┐      │
│  │  AI Provider │ │  Data Layer         │      │
│  │  (接口抽象)   │ │  RiskRules          │      │
│  │  ┌─────────┐│ │  ProductDB          │      │
│  │  │MockAI   ││ │  NarrationTemplates │      │
│  │  │(当前)    ││ └────────────────────┘      │
│  │  └─────────┘│                              │
│  │  ┌─────────┐│                              │
│  │  │QwenAI   ││  ← 已接入                     │
│  │  │(真实识别)││                              │
│  │  └─────────┘│                              │
│  └─────────────┘                              │
└───────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│            AI Cloud API (Qwen)               │
│  Qwen3-VL  Qwen3.7-Plus  Fun-ASR            │
└─────────────────────────────────────────────┘
```

### 2.2 核心时序图

```
用户          移动端           后端API          Service          AI Provider
 │              │                │               │                 │
 │  点击开始挑战  │                │               │                 │
 │─────────────>│                │               │                 │
 │              │  POST /challenge/start         │                 │
 │              │───────────────>│               │                 │
 │              │  返回 challengeId              │                 │
 │              │<───────────────│               │                 │
 │              │                │               │                 │
 │  引导拍全景照  │                │               │                 │
 │<─────────────│                │               │                 │
 │              │                │               │                 │
 │  拍全景照     │                │               │                 │
 │─────────────>│                │               │                 │
 │              │  POST /scan/panorama           │                 │
 │              │  (image)       │               │                 │
 │              │───────────────>│ analyzePanorama()                │
 │              │                │──────────────>│ mock/ai分析     │
 │              │                │<──────────────│ 返回高风险区域列表│
 │              │  返回需细拍区域  │               │                 │
 │              │<───────────────│               │                 │
 │  引导细拍区域1 │                │               │                 │
 │<─────────────│                │               │                 │
 │              │                │               │                 │
 │  拍细拍照1    │                │               │                 │
 │─────────────>│                │               │                 │
 │              │  POST /scan/identify           │                 │
 │              │  (image, challengeId)          │                 │
 │              │───────────────>│               │                 │
 │              │  识别中...      │               │                 │
 │              │  POST /narration/generate      │                 │
 │              │───────────────>│ generateNarration()              │
 │              │  返回趣味旁白    │               │                 │
 │              │<───────────────│               │                 │
 │              │  显示放大镜动画+旁白            │                 │
 │              │                │ identifyProduct()                │
 │              │                │──────────────>│ mock/ai识别     │
 │              │                │<──────────────│ 返回产品信息     │
 │              │  返回识别草稿，用户核对/修改     │                 │
 │              │  POST /scan/confirm            │                 │
 │              │───────────────>│ assessRisk()                     │
 │              │                │──────────────>│ 本地风险评估      │
 │              │                │<──────────────│ 返回评估结果      │
 │              │  返回识别结果+风险              │                 │
 │              │<───────────────│               │                 │
 │  显示结果/雷点 │                │               │                 │
 │<─────────────│                │               │                 │
 │              │                │               │                 │
 │  ... 重复细拍其他区域 ...        │               │                 │
 │              │                │               │                 │
 │  点击排雷完毕  │                │               │                 │
 │─────────────>│                │               │                 │
 │              │  POST /challenge/result         │                 │
 │              │───────────────>│ generateReport()                 │
 │              │                │──────────────>│ 生成报告+评分    │
 │              │  返回排雷报告    │               │                 │
 │              │<───────────────│               │                 │
 │  显示评分+分享 │                │               │                 │
 │<─────────────│                │               │                 │
```

---

## 三、后端架构

### 3.1 目录结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                      # FastAPI 应用入口，注册路由
│   ├── config.py                    # 配置管理（环境变量、AI开关等）
│   │
│   ├── api/                         # API 路由层
│   │   ├── __init__.py
│   │   ├── deps.py                  # 依赖注入（DB session、AI provider等）
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── challenge.py         # 挑战相关路由
│   │       ├── scan.py              # 扫描识别路由
│   │       └── narration.py         # 趣味旁白路由
│   │
│   ├── core/                        # 核心抽象层
│   │   ├── __init__.py
│   │   ├── ai_provider.py           # AI接口抽象基类（ABC）
│   │   ├── mock_ai.py               # Mock AI实现（当前使用）
│   │   ├── qwen_ai.py               # Qwen AI实现（后期接入，暂为空壳）
│   │   └── risk_engine.py           # 风险评估引擎
│   │
│   ├── models/                      # 数据模型（Pydantic）
│   │   ├── __init__.py
│   │   ├── challenge.py             # 挑战相关模型
│   │   ├── scan.py                  # 扫描相关模型
│   │   ├── risk.py                  # 风险相关模型
│   │   └── report.py               # 报告相关模型
│   │
│   ├── services/                    # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── scan_service.py          # 扫描业务逻辑
│   │   ├── risk_service.py          # 风险评估业务逻辑
│   │   ├── report_service.py        # 报告生成业务逻辑
│   │   └── narration_service.py     # 趣味旁白生成
│   │
│   ├── data/                        # 静态数据
│   │   ├── risk_rules.json          # 化学品冲突规则库
│   │   ├── product_risks.json       # 产品风险数据库
│   │   ├── category_risks.json      # 品类通用风险模板
│   │   └── narration_templates.json # 趣味旁白模板
│   │
│   └── utils/                       # 工具函数
│       ├── __init__.py
│       ├── image.py                 # 图片处理工具
│       └── scoring.py               # 评分计算工具
│
├── tests/                           # 测试（写完即删）
│   ├── __init__.py
│   ├── test_risk_engine.py
│   ├── test_scan_service.py
│   └── test_api.py
│
├── requirements.txt
├── .env.example                     # 环境变量模板
└── README.md
```

### 3.2 分层职责

| 层 | 职责 | 禁止 |
|---|---|---|
| **api/routes/** | 接收HTTP请求，参数校验，调用Service，返回JSON | 不直接操作数据库，不直接调AI |
| **services/** | 业务逻辑编排，调用core和data | 不处理HTTP细节 |
| **core/** | AI接口抽象、风险评估引擎 | 不含业务流程 |
| **models/** | Pydantic数据模型定义 | 不含逻辑 |
| **data/** | 静态数据文件 | 不含代码 |
| **utils/** | 纯函数工具 | 不含状态 |

### 3.3 AI接口抽象层设计

```python
# app/core/ai_provider.py

from abc import ABC, abstractmethod
from app.models.scan import PanoramaResult, ProductIdentification
from app.models.risk import RiskAssessment
from app.models.report import ReportData

class AIProvider(ABC):
    """AI能力抽象接口。当前由MockAI实现，后期由QwenAI实现。"""

    @abstractmethod
    async def analyze_panorama(self, image_bytes: bytes) -> PanoramaResult:
        """分析全景照，返回需细拍的高风险区域列表"""
        pass

    @abstractmethod
    async def identify_product(self, image_bytes: bytes) -> ProductIdentification:
        """识别细拍照中的产品信息"""
        pass

    @abstractmethod
    async def assess_risk(self, products: list[ProductIdentification]) -> RiskAssessment:
        """风险评估：单品风险 + 交叉风险"""
        pass

    @abstractmethod
    async def generate_narration(self, context: dict) -> str:
        """生成识别等待期间的趣味旁白"""
        pass

    @abstractmethod
    async def generate_report(self, scan_records: list) -> ReportData:
        """生成最终排雷报告"""
        pass
```

### 3.4 Mock AI实现策略

MockAI返回预设的合理数据，确保前端可以完整走通全流程：

| 方法 | Mock返回 |
|---|---|
| analyze_panorama | 返回3个区域：右侧柜子（高风险）、台面喷雾（中风险）、角落洗衣液（低风险） |
| identify_product | 根据图片hash随机返回预设产品（84消毒液/洁厕灵/驱蚊液/洗发水等） |
| assess_risk | 检查已识别产品列表，触发预设冲突规则（如84+洁厕灵=高危雷） |
| generate_narration | 从narration_templates.json随机取2-3条 |
| generate_report | 统计雷点数量，计算评分 |

### 3.5 API设计

#### POST /challenge/start
```json
// Request
{}

// Response
{
  "challenge_id": "uuid-xxx",
  "guide_message": "好，挑战开始！先拍一张你家最可能有化学品的地方——厨房水槽下面、卫生间柜子，都是重灾区。",
  "created_at": "2026-07-10T20:00:00Z"
}
```

#### POST /scan/panorama
```json
// Request: multipart/form-data
// image: <图片文件>
// challenge_id: "uuid-xxx"

// Response
{
  "scan_id": "scan-xxx",
  "areas": [
    {
      "area_id": "area-1",
      "description": "右侧水槽下方柜子",
      "items_hint": "多瓶液体容器",
      "risk_level": "high",
      "guide_message": "右边柜子下面有好几个瓶子，靠近拍一下最前面那个。"
    },
    {
      "area_id": "area-2",
      "description": "左侧台面",
      "items_hint": "喷雾罐",
      "risk_level": "medium",
      "guide_message": "台面上那个喷雾也拍一下，看着像驱蚊液。"
    }
  ],
  "guide_message": "我看到了2个需要检查的区域，逐个靠近拍一下。"
}
```

#### POST /scan/identify
```json
// Request: multipart/form-data
// image: <图片文件>
// challenge_id: "uuid-xxx"
// area_id: "area-1"

// Response（识别草稿，尚未进行风险判断）
{
  "draft_id": "draft-xxx",
  "challenge_id": "uuid-xxx",
  "area_id": "area-1",
  "product": {
    "brand": "蓝月亮",
    "name": "亮白增艳洗衣液",
    "category": "洗衣液",
    "ingredients": ["表面活性剂", "香精"],
    "confidence": "high"
  },
  "requires_review": false,
  "guide_message": "请核对识别结果，确认后再进行风险评估。"
}
```

#### POST /scan/confirm
```json
// Request: application/json
{
  "challenge_id": "uuid-xxx",
  "draft_id": "draft-xxx",
  "product": {
    "brand": "威猛先生",
    "name": "洁厕灵",
    "category": "洁厕剂",
    "ingredients": ["盐酸"],
    "confidence": "high"
  }
}

// Response（确认后才进行风险评估并写入报告）
{
  "scan_id": "scan-xxx",
  "status": "mine",
  "confirmed_by_user": true,
  "product": { "brand": "威猛先生", "name": "洁厕灵", "category": "洁厕剂", "ingredients": ["盐酸"], "confidence": "high" },
  "risk": {
    "level": "critical",
    "type": "化学品冲突",
    "description": "含氯消毒剂与酸性洁厕剂混用可能释放有毒氯气。",
    "advice": "分开存放和使用，不要混合。"
  },
  "guide_message": "这是今天的第一个雷。继续检查。"
}
```

#### POST /narration/generate
```json
// Request
{
  "challenge_id": "uuid-xxx",
  "context": {
    "current_area": "右侧水槽下方柜子",
    "scan_count": 2
  }
}

// Response
{
  "narrations": [
    "这个成分表字也太小了吧……让我凑近看看……",
    "嗯，表面活性剂，这个我认识……等等，后面还跟了一个什么？"
  ]
}
```

#### POST /challenge/result
```json
// Request
{
  "challenge_id": "uuid-xxx"
}

// Response
{
  "score": 82,
  "level": "基本安全，但有几个角落要收拾了",
  "total_mines": 2,
  "mine_breakdown": {
    "critical": 1,
    "medium": 0,
    "low": 1
  },
  "mines": [
    {
      "products": ["84消毒液", "洁厕灵"],
      "type": "化学品冲突",
      "level": "critical",
      "description": "混用产生氯气",
      "advice": "分开存放"
    }
  ],
  "share_text": "我家排雷82分，发现2个雷！你家能拿多少分？来挑战→",
  "safe_items": 3
}
```

---

## 四、移动端架构

### 4.1 目录结构

```
mobile/
├── src/
│   ├── screens/                        # 页面
│   │   ├── HomeScreen.tsx              # 首页：标题+开始挑战按钮
│   │   ├── ScanScreen.tsx              # 排雷页面：拍照+识别+结果
│   │   └── ResultScreen.tsx            # 结果页：评分+报告+分享
│   │
│   ├── components/                     # 可复用组件
│   │   ├── CameraCapture.tsx           # 拍照组件
│   │   ├── MagnifierAnimation.tsx      # 放大镜小人动画
│   │   ├── NarrationBubble.tsx         # 趣味旁白气泡
│   │   ├── MineCard.tsx                # 雷点卡片
│   │   ├── SafeTag.tsx                 # 安全标签
│   │   ├── ScoreBoard.tsx              # 评分展示
│   │   ├── GuideMessage.tsx            # AI引导消息
│   │   └── ProgressIndicator.tsx       # 进度指示器
│   │
│   ├── services/                       # 服务层
│   │   ├── api.ts                      # API客户端（真实接口）
│   │   └── mockApi.ts                  # Mock接口（移动端独立开发用）
│   │
│   ├── store/                          # 状态管理
│   │   └── challengeStore.ts           # Zustand store
│   │
│   ├── types/                          # TypeScript类型定义
│   │   └── index.ts
│   │
│   ├── theme/                          # 主题
│   │   ├── colors.ts
│   │   └── spacing.ts
│   │
│   └── utils/                          # 工具
│       ├── image.ts                    # 图片处理（压缩等）
│       └── share.ts                    # 分享功能
│
├── App.tsx                             # 应用入口+导航
├── app.json                            # Expo配置
├── package.json
├── tsconfig.json
└── babel.config.js
```

### 4.2 页面状态流转

```
HomeScreen
  │
  │ 用户点击"开始挑战"
  │ POST /challenge/start → 获取 challengeId
  ▼
ScanScreen
  │
  ├── 状态1：PANORAMA（拍全景）
  │     显示引导："拍一张你家最可能有化学品的地方"
  │     用户拍照 → POST /scan/panorama → 获取areas列表
  │     → 切换到状态2
  │
  ├── 状态2：GUIDE（引导细拍）
  │     显示当前要拍的区域："靠近拍右边柜子上的瓶子"
  │     用户拍照 → 切换到状态3
  │
  ├── 状态3：ANALYZING（识别中）
  │     POST /narration/generate → 获取趣味旁白
  │     POST /scan/identify → 获取识别草稿
  │     同时显示放大镜小人动画+趣味旁白
  │     识别完成 → 用户核对/修改 → POST /scan/confirm
  │     风险评估完成 → 切换到状态4
  │
  ├── 状态4：RESULT（单次结果）
  │     安全 → 显示安全标签 → "继续拍下一个"
  │     踩雷 → 弹出雷点卡片 → "继续排雷"
  │     → 还有未拍区域 → 切换到状态2
  │     → 全部拍完 → 显示"排雷完毕"按钮
  │     → 用户点击 → 切换到ResultScreen
  │
  ▼
ResultScreen
  │
  │ POST /challenge/result → 获取评分+报告
  │ 显示评分、雷点列表、排雷建议
  │ 分享按钮 → 生成分享卡片
  │ "再排一次" → 返回HomeScreen
```

### 4.3 状态管理（Zustand）

```typescript
// store/challengeStore.ts

interface ChallengeState {
  // 挑战状态
  challengeId: string | null;
  status: 'idle' | 'panorama' | 'guide' | 'analyzing' | 'single_result' | 'done';

  // 全景扫描结果
  areas: Area[];
  currentAreaIndex: number;

  // 识别结果
  scanResults: ScanResult[];
  mineCount: number;

  // 最终报告
  report: Report | null;

  // Actions
  startChallenge: (id: string) => void;
  setPanoramaResult: (areas: Area[]) => void;
  addScanResult: (result: ScanResult) => void;
  nextArea: () => void;
  setReport: (report: Report) => void;
  reset: () => void;
}
```

### 4.4 放大镜小人动画方案

| 方案 | 实现 | 优缺点 |
|---|---|---|
| Lottie动画 | 设计师导出JSON，react-lottie播放 | 效果最好，但需要动画素材 |
| CSS动画+图片 | 小人图片+CSS translate动画 | 简单，AI可生成代码，效果够用 |
| SVG动画 | react-native-svg + Animated API | 灵活但复杂 |

**当前选择**：CSS动画+图片方案。用一张放大镜小人PNG，通过Animated API让它在物品图片上左右移动+轻微缩放，配合趣味旁白逐句显示。后期有设计师素材可替换为Lottie。

---

## 五、数据文件设计

### 5.1 化学品冲突规则库 (risk_rules.json)

```json
[
  {
    "id": "RULE_001",
    "name": "氯气产生",
    "trigger": {
      "ingredients_1": ["次氯酸钠", "含氯漂白剂"],
      "ingredients_2": ["盐酸", "酸性物质"]
    },
    "level": "critical",
    "title": "致命组合！",
    "description": "84消毒液和洁厕灵混用会产生氯气——一战时的化学武器。2026年6月就有宝妈因此进了急诊。",
    "advice": "立刻分开存放。使用时绝对不要同时同地使用。"
  },
  {
    "id": "RULE_002",
    "name": "氯胺气体",
    "trigger": {
      "ingredients_1": ["含氯漂白剂"],
      "ingredients_2": ["氨", "含氨清洁剂"]
    },
    "level": "critical",
    "title": "有毒气体警告",
    "description": "含氯漂白剂和含氨清洁剂混合会产生氯胺气体，吸入可导致呼吸道损伤。",
    "advice": "分开存放，切勿混合使用。"
  },
  {
    "id": "RULE_003",
    "name": "DEET婴幼儿风险",
    "trigger": {
      "ingredients_1": ["避蚊胺", "DEET"],
      "condition": "家有婴幼儿"
    },
    "level": "medium",
    "title": "儿童风险",
    "description": "避蚊胺（DEET）对婴幼儿神经系统有潜在风险，2岁以下儿童不建议使用。",
    "advice": "为儿童选择无DEET的驱蚊产品，如派卡瑞丁或天然成分驱蚊液。"
  }
]
```

### 5.2 趣味旁白模板 (narration_templates.json)

```json
{
  "start": [
    "让我看看这是什么……",
    "凑近一点……再近一点……",
    "这个瓶子看着普普通通的样子……"
  ],
  "analyzing": [
    "这个成分表字也太小了吧……",
    "嗯，表面活性剂，这个我认识……",
    "等等，后面还跟了一个什么？",
    "让我查查我的数据库……",
    "这个组合有点意思……",
    "咦，这个成分我在去年的检测报告里见过……",
    "转个角度看下背面……",
    "这瓶子的颜色不太对劲啊……"
  ],
  "found_something": [
    "叮！找到了。你猜怎么着——",
    "嗯……这个有意思了。",
    "等一下，我发现了点东西……",
    "果然！我就觉得这东西不对劲。"
  ],
  "all_clear": [
    "呼——虚惊一场。",
    "这个没毛病，下一个。",
    "安全！继续继续。"
  ]
}
```

### 5.3 品类通用风险模板 (category_risks.json)

```json
[
  {
    "category": "含氯消毒剂",
    "default_risk": "medium",
    "risk_note": "含氯消毒剂有腐蚀性和刺激性，需稀释后使用，不可与酸性产品混用。"
  },
  {
    "category": "洁厕剂",
    "default_risk": "medium",
    "risk_note": "洁厕剂通常含强酸，有腐蚀性，使用时戴手套，避免与含氯产品混用。"
  },
  {
    "category": "驱蚊液",
    "default_risk": "low",
    "risk_note": "注意查看是否含DEET（避蚊胺），婴幼儿和孕妇需谨慎使用。"
  }
]
```

---

## 六、部署方案

### 6.1 开发环境

```
开发机
├── 后端：localhost:8000（FastAPI dev server）
├── 移动端：localhost:8081（Expo dev server）
└── 手机：Expo Go App 扫码连接
```

手机和开发机需在同一局域网。Expo Go自动加载最新代码。

### 6.2 测试环境（APK打包）

**后端**：
- 部署到云服务器（或使用ngrok内网穿透）
- FastAPI + uvicorn，端口8000
- 环境变量配置AI开关（当前MOCK=true）

**移动端**：
- `eas build -p android --profile preview` 生成APK
- 或本地 `expo run:android` 直接编译
- APK安装到Android手机测试

### 6.3 生产部署（比赛Demo）

**后端**：
- Docker容器部署到云服务器
- 配置环境变量：AI_PROVIDER=qwen、QWEN_API_KEY、QWEN_BASE_URL 与模型名
- Nginx反代，HTTPS

**移动端**：
- `eas build -p android --profile production` 生成签名APK
- 或通过Expo OTA更新热推送
- 分发APK给评委安装

### 6.4 AI接入切换

只需修改后端环境变量：
```bash
# .env（真实密钥仅保存在本机或部署平台）
AI_PROVIDER=qwen
QWEN_API_KEY=<secret>
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_VL_MODEL=qwen3-vl-plus
QWEN_TEXT_MODEL=qwen-flash
```

代码中通过依赖注入切换：
```python
# app/api/deps.py
def get_ai_provider() -> AIProvider:
    if settings.AI_PROVIDER == "qwen":
        return QwenAI()
    return MockAI()
```

---

## 七、开发计划与进度追踪

### 7.1 开发阶段

| 阶段 | 内容 | 预计时间 | 状态 |
|---|---|---|---|
| Phase 1 | 后端骨架：项目结构+FastAPI+路由+数据模型 | 2天 | ✅ 完成 |
| Phase 2 | Mock AI实现：5个方法全部返回合理Mock数据 | 2天 | ✅ 完成 |
| Phase 3 | 后端业务逻辑：风险评估引擎+评分逻辑+规则匹配 | 3天 | ✅ 完成 |
| Phase 4 | 移动端骨架：Expo项目+导航+三个页面框架 | 2天 | ✅ 完成 |
| Phase 5 | 移动端核心：拍照+API对接+状态管理 | 3天 | ✅ 完成 |
| Phase 6 | 移动端体验：放大镜动画+雷点卡片+引导消息 | 3天 | ✅ 完成 |
| Phase 7 | 联调：前后端联调+Mock数据走通全流程 | 2天 | 未开始（明天手机联调） |
| Phase 8 | 打磨：UI细节+评分文案+分享卡片 | 3天 | ✅ 完成（首轮） |
| Phase 9 | 部署：APK打包+后端部署+测试 | 2天 | 部署文档完成，待执行 |
| Phase 10 | AI接入：QwenAI实现+联调测试 | 3天 | 🚧 进行中（架构已确认） |
| Buffer | 预留缓冲 | 5天 | - |

### 7.2 功能完成度追踪

| 功能 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 后端项目骨架 | P0 | ✅ 完成 | |
| API路由定义 | P0 | ✅ 完成 | |
| 数据模型定义 | P0 | ✅ 完成 | |
| MockAI - analyze_panorama | P0 | ✅ 完成 | |
| MockAI - identify_product | P0 | ✅ 完成 | |
| MockAI - assess_risk | P0 | ✅ 完成 | |
| MockAI - generate_narration | P0 | ✅ 完成 | |
| MockAI - generate_report | P0 | ✅ 完成 | |
| 风险评估引擎 | P0 | ✅ 完成 | |
| 评分计算逻辑 | P0 | ✅ 完成 | |
| 化学品冲突规则库 | P0 | ✅ 完成 | |
| 趣味旁白模板库 | P0 | ✅ 完成 | |
| 移动端Expo项目 | P0 | ✅ 完成 | |
| 首页 | P0 | ✅ 完成 | |
| 排雷页面-全景拍照 | P0 | ✅ 完成 | |
| 排雷页面-细拍引导 | P0 | ✅ 完成 | |
| 排雷页面-识别等待动画 | P0 | ✅ 完成 | |
| 排雷页面-雷点卡片 | P0 | ✅ 完成 | |
| 排雷页面-安全反馈 | P0 | ✅ 完成 | |
| 结果页-评分展示 | P0 | ✅ 完成 | |
| 结果页-雷点列表 | P0 | ✅ 完成 | |
| 结果页-分享卡片 | P1 | ✅ 完成 | |
| Zustand状态管理 | P0 | ✅ 完成 | |
| API客户端 | P0 | ✅ 完成 | |
| QwenAI实现 | P2 | ✅ 完成 | 真实图片全流程已联调，19项后端测试通过 |
| Android APK打包 | P0 | 未开始 | |
| 后端部署 | P1 | 未开始 | |

---

## 八、编码规范

### 8.1 通用规范

1. **任何代码变更前，先更新本文档对应部分**
2. **测试代码写完验证后立即删除，不留在代码库中**
3. **每个文件单一职责，不超过300行**
4. **函数命名用动词开头，变量名用名词**
5. **后端类型注解全覆盖，移动端TypeScript严格模式**
6. **API响应直接返回数据对象，错误通过HTTP状态码+FastAPI异常处理返回**

### 8.2 Git提交规范

```
feat: 新功能
fix: 修bug
refactor: 重构
docs: 文档更新（含架构方案更新）
chore: 杂项
test: 测试（提交后应删除测试代码）
```

### 8.3 文件大小限制

| 类型 | 最大行数 | 超出怎么办 |
|---|---|---|
| 后端单个路由文件 | 150行 | 拆分到多个route文件 |
| 后端单个Service | 200行 | 拆分Service |
| 移动端单个页面 | 250行 | 拆分子组件 |
| 移动端单个组件 | 150行 | 拆分子组件 |

---

## 九、待解决问题

| 问题 | 状态 | 说明 |
|---|---|---|
| 图片压缩 | 已定 | 使用expo-image-manipulator，压缩到1280px宽、quality 0.7后上传 |
| 离线模式 | 不做 | 比赛Demo阶段不需要离线 |
| 用户账号系统 | 不做 | Demo阶段不需要登录，challengeId用本地存储 |
| 多语言 | 不做 | 中文only |
| 放大镜小人素材 | 已优化 | 用Emoji🔍+Animated动画（移动+缩放+旋转+扫描线+物品轮廓），效果够用 |
| Expo账号注册 | 待办 | EAS配置文件已写好(eas.json)，待注册账号后执行打包 |
| React Navigation安装 | 待办 | 需安装@react-navigation/native + native-stack |
| 无毒先锋数据获取 | 待确认 | 需要爬取/整理澎湃新闻文章中的数据 |

---

## 十、Qwen 真实模型接入架构（2026-07-13）

### 10.1 职责与信任边界

真实模型接入遵循“模型做观察，规则做裁决”：

1. `QwenAI.analyze_panorama` 使用 `qwen3-vl-plus` 识别值得细拍的区域，仅输出位置、物品提示和预判优先级。
2. `QwenAI.identify_product` 使用 `qwen3-vl-plus` 完成包装文字/OCR、产品名、品类和成分提取。
3. `QwenAI.assess_risk` 不请求云模型，始终调用本地 `RiskEngine`；风险等级、冲突和证据来源必须来自版本化规则库。
4. 最终报告的分数、雷点统计和分享文案采用确定性本地构建器，避免同一批扫描结果得到不同分数。
5. 趣味旁白可使用 `qwen-flash` 生成，但不得给出医学诊断、化学安全结论或替代产品说明书。

### 10.2 调用链

```text
手机图片
  -> FastAPI 图片类型/大小校验
  -> 内存 Base64 Data URL（不落盘）
  -> Qwen3-VL 结构化 JSON
  -> Pydantic 严格校验
  -> ProductIdentification
  -> 本地 RiskEngine + 权威证据库
  -> ScanResult
```

### 10.3 配置

后端通过 OpenAI-compatible API 调用模型，密钥只存在于 `backend/.env` 或部署平台密钥管理中：

```env
AI_PROVIDER=qwen
QWEN_API_KEY=<仅在本机配置，不提交>
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_VL_MODEL=qwen3-vl-plus
QWEN_TEXT_MODEL=qwen-flash
QWEN_TIMEOUT_SECONDS=30
QWEN_MAX_RETRIES=1
```

区域与 API Key 必须匹配。移动端代码、数据库、日志和 API 响应均不得包含密钥。

### 10.4 结构化输出与失败策略

- 所有模型提示词明确要求 JSON，调用启用 `response_format=json_object` 并关闭思考输出。
- 模型结果先经过 Pydantic 校验；空响应、非 JSON、缺字段或非法枚举统一视为上游模型失败。
- 超时返回 `504 AI_TIMEOUT`，连接错误、模型拒绝或结构错误返回 `502 AI_PROVIDER_ERROR`，响应保留请求追踪 ID。
- `AI_PROVIDER=qwen` 时禁止静默回退到 Mock 数据，避免真实扫描被伪造演示结果污染。
- 识别置信度为 `low` 时允许展示结果，但 UI 必须提示用户重拍或核对包装标签。

### 10.5 隐私、日志与成本

- 图片只在一次请求生命周期内驻留内存，不写 SQLite、不写文件、不进入应用日志。
- 日志只记录 provider、模型名、调用类型、耗时和成功/失败，不记录图片、提示词全文、模型原文或密钥。
- 自 10.27 单场景快速检查起，全景最多返回 3 个区域；旁白最多 3 条；客户端与服务端继续执行图片压缩、扫描配额和频率限制。
- 自动化测试全部使用假客户端，不访问公网、不消耗额度；真实联调必须显式执行一次最小请求。

### 10.6 验收标准

- Mock 模式原有全流程测试继续通过。
- Qwen 模式覆盖：有效全景 JSON、有效产品 JSON、非法 JSON、超时、缺少密钥。
- 配置检查只能输出是否已配置，不得打印密钥。
- 完成后执行后端测试、Python 编译、移动端 TypeScript 检查和 Expo Web 构建。

### 10.7 相册图片输入（2026-07-13）

- 全景扫描和单品细拍入口都同时提供“拍照”和“从相册选择”两个动作，二者进入同一 `handleCapture(uri)` 分析流程。
- 使用 Expo SDK 57 的 `expo-image-picker`，只允许单选图片，不读取视频、不批量读取相册。
- Web 端由用户点击按钮直接触发系统文件选择器，兼容电脑选择本地照片；Android/iOS 使用系统照片选择界面。
- 用户取消选择时保持当前页面和扫描进度，不显示错误；选择器异常才显示可重试提示。
- 相册图片继续经过现有的 1280px、JPEG quality 0.7 压缩，再上传后端；原图不写应用数据库或日志。
- 验收覆盖全景入口、细拍入口、取消选择、TypeScript 检查与 Expo Web 构建。

### 10.8 基于原图的识别等待界面（2026-07-13）

- `ScanScreen` 在提交扫描前把本次拍摄/选择的图片 URI 暂存为 `analysisImageUri`，仅在 `analyzing` 状态使用。
- `MagnifierAnimation` 以该图片作为 `cover` 背景，让扫描线和放大镜直接在待分析照片上运动。
- 图片上增加半透明暗色蒙层与扫描框，保证浅色、深色照片上旁白和加载状态都清晰可读。
- 请求成功或失败离开分析状态后立即清空 URI 引用；不新增持久化、不把图片写入挑战状态或数据库。
- 图片加载失败或 URI 缺失时保留原来的中性背景，避免识别流程被展示层阻断。

### 10.9 全景图区域定位引导（2026-07-13）

- `PanoramaArea` 新增可选字段 `bbox_2d: [x1, y1, x2, y2]`，采用 Qwen3-VL 官方的 `0–999` 归一化坐标，原点在图片左上角。
- 后端提示词要求每个待细拍区域同时返回描述与边界框；Pydantic 校验坐标范围、顺序和最小面积，非法框降级为上游结构错误。
- 全景图片 URI 只保存在 `ScanScreen` 当前会话内，不写 Zustand、SQLite、日志或历史记录；完成挑战或页面卸载后释放。
- `GuideView` 显示原全景图：当前区域使用橙色实线框和“请拍这里”序号，后续区域使用低透明度虚线框，文字描述作为辅助信息保留。
- 图片容器按原图宽高比展示，坐标按百分比映射，避免 Web、Android、iOS 不同尺寸造成框位移。
- 兼容旧数据与模型偶发缺框：无 `bbox_2d` 时仍显示全景图和文字说明，不影响拍照、相册选择和跳过流程。

### 10.10 模型请求超时对齐（2026-07-13）

- 实测 Qwen3-VL 全景识别约需 16–17 秒，原移动端统一 15 秒超时会先于后端成功响应触发，导致页面错误退回并诱发重复请求。
- 普通 API 与旁白继续使用 15 秒超时；全景识别和单品 OCR 使用独立的 45 秒客户端超时。
- 客户端模型超时必须大于后端 `QWEN_TIMEOUT_SECONDS=30` 并预留网络传输时间，确保后端能先返回结构化 `504 AI_TIMEOUT`。
- 分接口超时由 API 客户端集中管理，页面不硬编码具体秒数。

### 10.11 全景区域数量控制（2026-07-13）

- Qwen 提示词从“最多 5 个”改为“只返回值得细拍的区域，普通场景优先 2–3 个；没有明确目标允许 0 个”。
- 相同物品、距离很近或边界框高度重叠的目标应合并为一个区域，减少重复拍摄。
- 后端按 `high > medium > low` 排序，并对 IoU 高于 0.65 的区域去重，避免完全依赖模型遵守数量要求。
- 普通场景最多保留 3 个区域；只有至少 4 个区域被模型明确标为 `high` 时，才允许最多 5 个。
- 全局引导文案由筛选后的实际数量生成，避免模型原文仍提示“共 5 个”而页面只展示 3 个。

### 10.12 零区域结果与可恢复交互（2026-07-13）

- “允许 0 个”仅适用于图片模糊、严重遮挡、纯空场景或完全没有瓶罐/喷雾/包装容器；只要存在疑似家庭化学品容器，Qwen 必须返回 1–3 个候选区域。
- 后端只记录 `raw_count`、`selected_count` 等数量元数据，不记录图片、区域描述或模型原文，方便区分模型空结果与客户端故障。
- 前端新增 `panorama_empty` 状态：保留本次原图，明确说明未定位到可细拍区域，并提供“重新分析这张照片”“重新拍照”“从相册换一张”。
- 空结果不再弹窗后立即退回初始全景页，避免用户误以为请求失败。
- 重新分析会产生一次新的模型调用，按钮文案需明确；更换图片不自动调用，仍由用户选择后提交。

### 10.13 产品识别确认与纠错（2026-07-13）

#### 流程拆分

```text
细拍图片
  -> POST /api/scan/identify
  -> Qwen 包装/OCR
  -> IdentificationDraft（不做风险判断、不计入报告）
  -> 用户核对/编辑品牌、产品名、品类、成分
  -> POST /api/scan/confirm
  -> 本地 RiskEngine
  -> 持久化 ScanResult（confirmed_by_user=true）
```

- 移除一步式 `/api/scan/detail`，防止真实模式绕过用户确认直接产生安全结论。
- `ChallengeState.pending_identifications` 只保存草稿 ID、区域 ID、结构化产品字段和创建时间，不保存原图。
- 同一区域重新识别时替换旧草稿；确认成功后删除草稿，重复确认或伪造草稿 ID 返回明确错误。
- 扫描配额按已确认结果计算；识别请求仍受现有接口频率限制，避免反复调用付费模型。

#### 客户端状态

- 新增 `identification_review` 页面状态，展示本次细拍图、置信度、品牌、产品名、品类和成分。
- 所有置信度都必须由用户确认；`low` 或关键字段为“未知”时显示醒目重拍提示。
- 用户可以直接编辑识别字段，成分使用中文顿号/逗号分隔；确认按钮提交修正后的结构化数据。
- “重新拍照”和“从相册换一张”会生成新草稿，不会把旧草稿计入扫描历史或报告。

#### 可审计性

- `ScanResult` 与报告雷点增加 `confirmed_by_user`，区分已确认结果和历史兼容数据。
- 模型只负责观察/OCR；确认后的产品数据仍由本地规则库判定风险，用户无法编辑风险等级或证据来源。
- 接口测试覆盖识别不落报告、确认后落库、重复确认失败、低置信度草稿和图片不落盘。

### 10.14 GitHub Actions 持续集成（2026-07-13）

#### 触发与权限

- 工作流文件固定为 `.github/workflows/ci.yml`，在推送到 `main`、针对 `main` 的 Pull Request 以及手动触发时运行。
- 使用 GitHub 托管的 `ubuntu-latest` runner；工作流权限限制为 `contents: read`，不得授予写仓库、部署、Issue 或发布权限。
- 同一分支的新运行会取消旧运行，减少重复排队和无效资源消耗。

#### 检查职责

- 后端 Job 使用固定 Python 3.12，安装 `backend/requirements-dev.txt` 后执行 `python -m pytest -q`。
- 前端 Job 使用 Expo SDK 57 要求的 Node.js 22.13，基于锁文件执行 `npm ci`，随后运行 `npx tsc --noEmit` 和 `npx expo export --platform web`。
- `mobile/tsconfig.json` 明确排除 `dist` 与 `.expo` 生成目录，确保本地重复构建后仍能稳定执行类型检查。
- Python 与 npm 缓存分别以对应依赖文件为键；缓存只用于加速，构建不得依赖缓存必然存在。
- 后端、前端分为两个独立 Job，并行运行并分别显示结果，便于快速定位失败范围。

#### 密钥与外部服务边界

- CI 强制使用 `AI_PROVIDER=mock`，禁止读取开发者本机 `.env`，不配置 `QWEN_API_KEY`，不调用公网模型接口。
- 自动化测试继续使用假图片字节和 Mock/Fake Client；不得上传用户图片、真实数据库或扫描记录。
- 当前 CI 只负责验证，不执行 APK 发布、Docker 推送或生产部署；未来新增发布流程必须使用独立工作流、受保护环境和最小权限密钥。

#### 验收标准

- YAML 可被 GitHub Actions 正确识别，首次推送产生 Backend tests 与 Mobile checks 两个 Job。
- 本地先执行同等的 pytest、TypeScript 与 Expo Web 构建，全部通过后才允许提交工作流。
- README 显示 CI 状态入口；失败时以 GitHub 日志为准，不通过跳过测试或注入真实密钥规避。

### 10.15 main 分支保护（2026-07-13，已启用）

- 仓库经用户明确确认后改为公开仓库，并启用 GitHub Free 支持的分支保护。
- `main` 作为唯一默认稳定分支，普通协作者不得直接写入，代码变更必须通过 Pull Request 合并。
- 合并前必须通过 GitHub Actions 的 `Backend tests` 与 `Mobile checks`，并要求分支基于最新 `main` 重新验证。
- 当前项目允许 0 人审批，避免单人开发阶段无法自行合并；后续增加稳定协作者后改为至少 1 人审批。
- 启用会话解决要求，Pull Request 中未解决的审查讨论会阻止合并。
- 禁止强制推送和删除 `main`，防止覆盖或丢失稳定历史。
- 仓库管理员暂不受规则强制约束，保留故障恢复入口；进入多人协作或发布阶段后再评估启用管理员强制执行。
- 分支保护属于 GitHub 仓库设置，不存放密钥，也不改变本地运行、模型调用或用户数据处理方式。

### 10.16 移动端 UI 优化设计流程（2026-07-15）

- UI 改版按“用户流程 → 页面线框图 → 视觉基础约束 → 组件图 → 功能编码 → 最终视觉精修 → 视觉验收”推进。
- `用户流程.md` 是本轮 UI 优化的产品输入，记录入口、主任务、用户决策、异常恢复、完成出口和历史报告支线。
- 用户流程分为“当前实现（AS-IS）”与“目标体验（TO-BE）”：前者必须与代码一致，后者只描述交互目标，不提前绑定组件或视觉表现。
- 核心任务以手机端单手操作为优先：开始挑战后，用户在每个页面都应明确知道当前进度、下一动作、照片用途和退出后果。
- 相机拍摄与相册选图是等价输入，选择后汇合到同一分析流程；取消相机或相册不得丢失已有挑战进度。
- 全景零区域、模型超时、识别失败、确认失败、报告生成失败、历史加载失败都必须有原地恢复路径，不能把用户无提示地退回流程起点。
- 产品识别必须经过用户确认后才能进入风险判断；用户可修正产品事实字段，但不能编辑风险等级、证据或评分。
- 跳过区域必须计入进度但不伪造扫描结果；最后一个区域完成或跳过后进入报告生成，失败时保留已确认结果并允许重试。
- 离开未完成挑战前，目标体验需要二次确认并说明记录后果；历史列表要区分可打开的已完成报告和不可打开的未完成记录。
- 用户流程确认前，不新增页面、不调整导航、不拆分 UI 组件；线框确认后只锁定会影响组件结构的视觉基础，再决定组件边界。

### 10.17 手机端低保真线框图（2026-07-15）

- 线框图基于已确认的 `用户流程.md`，覆盖：首页、全景输入、分析、零区域、区域引导、识别确认、单品结果、报告、历史九组页面。
- 设计基准视口为 390×844，优先验证手机单手操作；Web 端后续只做响应式承载，不反向决定手机信息层级。
- 本阶段统一使用灰度、占位图和示意图形，不确定品牌色、插画风格、最终字体或动效，防止低保真评审被视觉偏好干扰。
- 每页最多一个高优先级主操作；相册、重拍、跳过、再次分析等动作按任务风险降为次操作或文字操作。
- 顶部区域固定承担返回/退出、页面标题和进度；底部优先放置当前任务主按钮，并预留手机安全区。
- 全景和单品照片是扫描流程的核心上下文，分析、零区域、区域引导和识别确认都必须保留可见图片。
- 状态反馈不能只依赖颜色或 Emoji；“需要注意”“当前规则未发现雷点”“资料待复核”等关键信息必须有文字。
- 加载、空数据、错误、无权限和退出确认作为同一页面的状态变体，不为每种异常新增导航页面。
- 主要触控目标按至少 44×44 设计；输入、按钮和风险说明按纵向阅读顺序排列，避免手机端横向拥挤。
- 线框图确认后先确定令牌结构、对比度、字号、间距、触控尺寸与状态语义，再进入组件图；最终美术风格留到功能编码完成后。

### 10.18 移动端视觉基础约束与最终精修（2026-07-16 修订）

- 编码前只锁定会影响组件 API 与布局的视觉基础：三层令牌结构、WCAG AA 对比度目标、系统字体与字号层级、4pt 间距、触控尺寸和状态语义。
- `#C83E13`、橙色品牌方向和当前样式板属于可访问的候选基线，不是最终美术定稿；最终色调可在功能编码完成后替换。
- 现有 `#FF6B35` 与白字的对比度约 2.84:1，禁止继续用于普通字号白字主按钮；无论最终选择何种品牌色，关键文字对比度必须达标。
- 视觉值采用“原始令牌 + 语义令牌 + 组件令牌”三层：业务页面只引用 `surface/text/action/border/risk` 等语义，基础组件可引用组件配方，任何业务文件都不新增十六进制颜色。
- 高风险、中风险、低风险和未命中规则分别使用红、橙、琥珀、绿，并始终配合文字标签与图标，不允许只通过颜色传达状态。
- 中文字体使用系统字体栈，优先保证 Android/iOS 一致性与加载稳定性；字号按 12/14/16/20/24/28/36 建立层级，正文默认 16/24。
- 间距采用 4pt 基础网格，令牌覆盖 4/8/12/16/20/24/32/40/48；主要页面水平边距 20，卡片内边距 16，区块间距 24。
- 圆角候选分为 8/12/16/24/全圆；按钮和输入高度分别不低于 52 与 48，触控区域不低于 44×44。
- 阴影、圆角细节、插画、图标、最终色板和动效氛围允许在最终视觉阶段调整，不得反向破坏已经确认的用户流程和组件语义。
- 图片采用 4:3 全景、16:9 单品基准比例；图片上的文字必须放在深色蒙层或实底标签上，不能直接压在复杂照片上。
- 动效数值为候选基线；最终动效仍必须支持系统“减少动态效果”并避免高频闪烁。
- 所有正文与关键控件按 WCAG AA 4.5:1 目标设计；大字最低 3:1，焦点、边界和状态不得只依赖色差。
- `移动端视觉规范.md` 与样式板现阶段作为“视觉基础约束 + 候选方向”评审产物；组件图只绑定语义令牌，不绑定候选色值。
- 功能编码和全部状态联调完成后，基于真实页面进行最终视觉定稿、视觉回归、长文本与真机验收。

### 10.19 前端组件分层与设计令牌映射（2026-07-16）

#### 分层边界

- 前端采用 `Tokens → Primitives → Composites → Feature Views → Screens` 五层结构，依赖只能由上层指向下层。
- `Tokens` 分为原始令牌、语义令牌和组件令牌三层；业务页面只能使用语义令牌或组件令牌，禁止直接新增十六进制颜色、任意间距和页面专属阴影。
- `Primitives` 只处理通用交互、可访问性和状态外观，不读取 Zustand、不调用 API、不理解“挑战、区域、雷点”等领域概念。
- `Composites` 组合基础控件并表达稳定的交互语义，例如照片输入、页面标题、进度摘要和状态提示；可以接收领域数据，但不发起网络请求。
- `Feature Views` 对应已确认用户流程中的页面状态，例如全景输入、区域引导、识别确认和单品结果；只通过属性与事件和页面控制器通信。
- `Screens` 负责导航、API 编排、全局状态和跨状态恢复，不重复绘制按钮、输入框、卡片和状态反馈。

#### 页面控制与状态所有权

- `HomeScreen` 负责开始挑战与进入历史记录；开始中的本地状态继续由页面持有。
- `ScanScreen` 保留扫描状态机的控制权，但把拍照来源、分析、区域引导、识别确认、单品结果和完成处理拆成可测试的功能视图。
- `ResultScreen` 负责读取报告、分享和重新挑战；评分、风险列表和空报告状态由展示组件负责。
- `HistoryScreen` 负责请求、刷新和打开报告；加载、空数据、错误和记录卡片使用统一状态组件与列表项。
- Zustand `challengeStore` 只保存跨页面、跨扫描步骤的挑战状态；输入框编辑值、相机开关、提交中等短生命周期状态留在对应页面或功能视图。
- API 客户端和图片选择服务保持在 `services` 层；基础组件与功能视图不得直接依赖服务实现。

#### 目标组件族

- 基础组件：`AppText`、`AppButton`、`IconButton`、`TextField`、`StatusBadge`、`Surface`、`ProgressBar`、`StateMessage`。
- 组合组件：`PageHeader`、`PhotoInputActions`、`PhotoFrame`、`RiskCard`、`ProductSummary`、`EvidenceStatus`、`ProgressSummary`、`ConfirmExitDialog`。
- 功能视图：`CameraCaptureView`、`PanoramaInputView`、`AnalysisView`、`PanoramaEmptyView`、`AreaGuideView`、`IdentificationReviewView`、`SingleResultView`、`ReportGeneratingView`、`ReportView`、`HistoryList`。
- 页面控制器：`HomeScreen`、`ScanScreen`、`ResultScreen`、`HistoryScreen`。
- 不创建万能 `Card`、万能 `Modal` 或大量布尔属性控制的“超级组件”；只有语义、行为和可访问性一致时才复用。

#### 迁移顺序

1. 建立三层令牌与旧主题兼容映射，不改变页面可见行为。
2. 实现文字、按钮、输入、状态标签、表面和状态提示等基础组件。
3. 以“全景输入 → 分析 → 零区域恢复”为第一条垂直切片，验证照片、加载与错误状态。
4. 迁移区域引导、识别确认和单品结果，统一照片框、进度、输入和风险表达。
5. 迁移报告、历史和首页，消除重复按钮、卡片与空错误状态。
6. 补齐无权限、退出确认、超时、重试、长文本和小屏状态。
7. 删除已无引用的旧样式，完成类型、单元、构建、截图和真机功能验收。
8. 功能全部稳定后再进入最终视觉精修，通过修改语义令牌值和组件配方完成风格定稿。

#### 评审与实施门禁

- `前端组件图与设计令牌映射.md`、HTML 图和 PNG 预览是本阶段评审产物。
- 组件图确认前不创建上述目标组件、不修改 `colors.ts`、`spacing.ts` 或现有页面代码。
- 组件图确认后，先在架构和开发日记记录第一条垂直切片，再进入功能编码。
- 每个迁移切片必须通过 TypeScript、现有测试、Expo Web 构建、关键状态截图和触控/长文本检查。

### 10.20 第一条功能垂直切片：全景输入与零区域恢复（2026-07-17）

#### 范围

- 本切片只迁移 `CameraCapture`、`PanoramaView`、`AnalysisView` 和 `EmptyPanoramaView` 所覆盖的“相机/相册输入 → 分析 → 零区域恢复”链路。
- `ScanScreen` 继续负责图片来源汇合、API 调用和 `panorama/analyzing/panorama_empty` 状态迁移，功能视图只接收数据与语义事件。
- 建立原始、语义、组件三层令牌和旧主题兼容别名；未迁移页面保持原有外观和行为。
- 只实现本切片需要的 `AppText`、`AppButton`、`Surface`、`StateMessage`、`PhotoFrame`、`PhotoInputActions`。
- 相机权限状态保留“授权相机”和“从相册选择”两条恢复路径；拍照中禁用取消和重复提交。
- 分析页面继续在用户原图上展示反馈，支持系统减少动态效果，文字始终位于实底或深色蒙层上。
- 零区域页保留原图，并继续提供“换一张、重新拍照、重新分析原图”三种动作；重新分析文案必须说明会再次调用模型。

#### 非目标

- 不修改后端接口、扫描状态枚举、Zustand 数据结构、图片压缩或隐私边界。
- 不迁移区域引导、识别确认、单品结果、报告、历史和首页。
- 不在本切片确定最终品牌色、插画、图标、阴影、圆角细节和动效风格。
- 不提前抽取 `useScanController`；只有第一条切片完成后仍存在明确重复或测试困难时再评估。

#### 兼容与回退

- 现有 `colors.ts`、`spacing.ts` 对外导出名称保持不变，内部改为引用新原始令牌，确保未迁移页面无需同步修改。
- 新组件只使用语义令牌和组件令牌；旧页面继续使用兼容别名，允许分切片迁移。
- 本切片不改变 API 请求顺序和页面状态条件；回退时可恢复旧视图导入，不影响后端或本地数据。

#### 验收

- 相机和相册仍汇合到同一个 `handleCapture`，取消选择不改变当前进度。
- 全景分析期间显示用户所选照片、分析状态和可朗读文本。
- 相机无权限、照片选择失败、分析失败和零区域均有明确恢复动作。
- 主要按钮高度不低于 52，其他触控目标不低于 44×44；加载与禁用状态通过 `accessibilityState` 暴露。
- 系统开启减少动态效果时停止循环位移、缩放、旋转和扫描线动画。
- TypeScript、后端测试、Expo Web 导出和 390×844/宽屏浏览器检查全部通过。

#### 实施结果

- 已建立 `tokens.ts`，分离原始令牌、语义令牌和组件令牌；旧 `colors.ts`、`spacing.ts` 保留兼容导出，未迁移页面行为不变。
- 已实现本切片所需的 `AppText`、`AppButton`、`Surface`、`StateMessage`、`PhotoFrame` 和 `PhotoInputActions`。
- `ScanScreen` 已切换到 `CameraCaptureView`、`PanoramaInputView`、`AnalysisView`、`PanoramaEmptyView` 和 `ReportGeneratingView`，API 与状态机条件未改变。
- 相机永久拒绝时，原生端引导打开系统设置；相册仍是可用的替代入口，Web 保留示例照片演示。
- 分析动画使用系统减少动态效果设置；原生端使用原生动画驱动，Web 使用 JS 驱动，避免浏览器降级警告。
- 零区域照片在手机端满宽、宽屏最大 520px；390×844 的三个操作高度为 52/52/44，1280×720 低高度宽屏可在内容区内部滚动。
- 最终验证结果：TypeScript 通过、Expo Web 导出成功、后端 19 项测试通过、浏览器运行日志无错误或警告。

### 10.21 第二条功能垂直切片：区域引导与产品识别确认（2026-07-17）

#### 范围

- 本切片迁移“原全景图区域引导 → 当前区域照片输入 → 产品识别中 → 产品事实核对 → 确认并进入本地风险评估”。
- `ScanScreen` 继续负责 API 编排、短生命周期错误和 `guide/analyzing/identification_review` 状态迁移；功能视图不调用 API、不读取 Zustand。
- `AreaGuideView` 使用统一的进度摘要、照片框、照片输入动作和状态提示；当前区域用实线和文字标签强调，后续区域用虚线和序号弱化。
- `PhotoFrame` 接收与领域无关的归一化标注数据；区域对象到标注数据的转换留在区域功能视图，基础组合组件不理解 `PanoramaArea`。
- 新增 `TextField`、`StatusBadge`、`ProgressSummary`，并扩展输入、状态标签、标注框和进度摘要所需的组件令牌。
- `IdentificationReviewView` 继续持有品牌、产品名、品类和成分的本地表单状态；确认事件只提交用户核对后的结构化产品事实。
- 产品名和品类为空时在字段附近显示明确错误并禁用确认；低置信度或需要复核时同时使用状态文字和说明，不只依赖颜色。
- 产品识别失败回到当前区域或旧草稿，并显示页面内恢复提示；确认失败停留在表单且保留用户编辑内容。

#### 非目标

- 不修改 `/scan/panorama`、`/scan/identify`、`/scan/confirm` 接口、请求顺序、图片压缩和隐私边界。
- 不修改 `ScanPageStatus`、Zustand 数据结构、风险引擎、扫描配额和报告持久化。
- 不迁移 `ResultView`、报告、历史和首页；这些属于后续垂直切片。
- 不抽取 `useScanController`，不新增第三方依赖，不确定最终色板、图标、插画、阴影和动效风格。
- 不把区域框坐标或原始图片写入 Zustand、SQLite、日志或历史记录。

#### 组件边界

- `TextField` 负责标签、必填标记、输入外观、帮助与错误关联，不负责产品字段业务校验。
- `StatusBadge` 负责“文字 + 状态语义”的一致表达，不计算识别置信度或风险等级。
- `ProgressSummary` 只展示当前区域、区域总数和已发现雷点数，不推进区域状态。
- `PhotoFrame` 负责图片、归一化标注和图片可访问说明，不读取区域或识别对象。
- `AreaGuideView` 负责把当前区域转换为展示信息和照片标注；拍照、相册和跳过均通过语义事件交给页面。
- `IdentificationReviewView` 负责本地编辑和字段校验；风险评估与确认请求仍由 `ScanScreen` 调用。

#### 兼容与回退

- 旧主题兼容导出继续保留，未迁移的单品结果、报告、历史和首页不受影响。
- `GuideView` 在新视图完成类型、构建和浏览器验证后再删除；回退只需恢复旧视图导入。
- 后端响应类型和 Store 数据保持不变，因此回退前端视图不会破坏本地挑战数据。
- 新组件只使用语义令牌和组件令牌，最终视觉阶段可通过替换令牌值调整风格。

#### 验收

- 390×844 手机视口中，当前区域、全景标注、拍照主操作、相册次操作和跳过操作均可滚动到达且不被遮挡。
- 有边界框时在原图准确展示当前和后续区域；缺少边界框时保留照片和文字引导。
- 相机、相册和跳过保持原有状态迁移；取消选择不改变当前区域和已确认结果。
- 单品分析以本次照片为背景；识别成功后展示照片、置信度、可编辑字段和规则边界。
- 识别失败、确认失败、字段缺失、低置信度、长文本和提交中均有明确且可访问的状态。
- TypeScript、后端测试、Expo Web 导出和手机/宽屏浏览器验证全部通过。

#### 实施结果

- 已新增 `TextField`、`StatusBadge` 和 `ProgressSummary`，输入、状态、进度和触控尺寸由组件令牌统一管理。
- `PhotoFrame` 已支持与领域无关的 `0–999` 归一化标注；`AreaGuideView` 只负责把区域对象转换为当前/后续标注。
- `GuideView` 与 `AnnotatedPanorama` 已由 `AreaGuideView` 和扩展后的 `PhotoFrame` 替代，确认无引用后删除。
- 区域优先级、已发现雷点和已确认产品均使用文字与状态符号，不再只显示风险色圆点或 Emoji。
- `IdentificationReviewView` 已迁移到统一照片框、状态标签、输入和按钮；产品名、品类缺失时显示就近错误并禁用确认。
- 单品识别失败显示在当前区域或旧草稿内；确认失败停留在识别表单，浏览器实测用户填写的产品名不会丢失。
- 390×844 下区域页三个操作高度为 52/52/44，识别页输入高度为 50/50/50/96，主要操作和恢复操作均满足触控目标。
- 1440×900 宽屏内容保持最大 720px，照片保持最大 520px，长表单在内容区内部滚动。
- 最终验证结果：TypeScript 通过、Expo Web 导出成功（539 个模块）、后端 19 项测试通过；临时 UI 验证服务已停止并删除。

### 10.22 最终视觉方向：高对比检验系统（2026-07-18）

#### 决策

- 产品方确认黑白作为最终视觉基础；根据视觉稿评审，将原先偏暖的“纸质档案感”调整为“高对比现代检验系统”：纯白、冷灰和近黑承担品牌、导航、主操作和页面结构，橙色候选品牌方向废止。
- 视觉稿评审否决额外电蓝信号色；界面保持纯黑白与冷灰，不再用单一高饱和色制造“科技感”。
- 当前步骤、扫描框、焦点、选中态和状态差异通过近黑实线、虚线、反白标签、字重和顺序表达；报告总览、历史摘要和分享卡同样保持完全黑白。
- 线框图已经确认的页面信息层级、照片优先、单主操作和恢复路径保持不变，本次变更不修改用户流程、API 契约、状态机或组件职责。
- 高风险、中风险、低风险、未命中规则和信息状态继续保留语义色，但报告总览、历史摘要和分享卡保持完全黑白，通过图标形状、完整文字、边框粗细和信息顺序表达状态；语义色只允许在展开后的风险详情、错误恢复和处理中局部出现，单屏彩色面积原则上不超过 5%。
- 首页、扫描、区域引导、识别确认、单品结果、报告和历史统一使用白底、近黑主文字、黑色主按钮、白底黑边次按钮、低圆角和无普通卡片阴影的视觉配方。
- 核心流程删除炸弹、庆祝等 Emoji，统一采用 2px 线性检验图标、编号、勾选和状态形状。
- 黑色幽默属于展示与传播层：允许出现在分析阶段提示、单品结果副标题、家庭化学品人格和分享卡；风险事实、反应说明、处置建议、证据、隐私和错误恢复文案保持严肃。

#### 令牌与组件落地

- `tokens.ts` 仍维持“原始令牌 → 语义令牌 → 组件配方”三层结构，只替换最终色值与圆角、边框、动效配方。
- 页面不得直接写入黑白或风险色值；`AppButton`、`Surface`、`StatusBadge`、`PhotoFrame` 等公共组件负责承接最终视觉。
- `colors.ts` 保留为未迁移页面的短期兼容层；首页、单品结果、报告和历史完成迁移后删除旧橙色、旧风险色和重复样式。
- 家庭化学品人格由受控模板和结构化类型生成，不在页面中散落硬编码；分享卡不得接收原图 URI、产品名称、地址或其他家庭隐私。

#### 迁移顺序

1. 评审首页、区域引导、结果报告三张最终视觉稿。
2. 更新最终原始令牌、语义令牌、组件配方和旧主题兼容映射。
3. 统一基础组件的黑白按钮、卡片、输入、状态、照片标注和减少动态效果变体。
4. 迁移首页并验证黑白品牌方向与手机首屏信息层级。
5. 迁移扫描链路剩余视觉，保证分析背景继续使用用户原图。
6. 迁移单品结果、最终报告与历史，加入受控的黑色幽默人格和隐私安全分享卡。
7. 删除无引用的旧主题与页面级视觉值，进入 390×844、小屏、宽屏和目标真机视觉 QA。

#### 非目标

- 不借视觉修改改变拍照、相册、区域数量、跳过、识别确认、报告生成和历史恢复逻辑。
- 不把娱乐人格标签作为风险判断依据，也不把品类识别描述为实验室检测结论。
- 不以高饱和红色、爆炸图形或伤害玩笑制造恐惧传播。

### 10.23 第三条功能垂直切片：最终令牌与首页迁移（2026-07-18）

#### 决策

- 本切片将纯白、冷灰和近黑作为首轮实现基线，但具体色值不冻结；后续只能通过原始令牌、语义令牌和组件配方统一调整，不允许页面写入十六进制颜色。
- `tokens.ts` 继续作为唯一新主题来源；`colors.ts`、`spacing.ts` 只保留旧页面兼容别名，避免尚未迁移的扫描结果、报告和历史页面同时重写。
- `HomeScreen` 继续负责开始挑战、开始失败和导航；页面外观改用 `AppText`、`AppButton`、`Surface` 与语义令牌，不创建只服务首页的万能卡片。
- 首页删除炸弹 Emoji、橙色下划线、彩色圆形和装饰阴影，改为“标题 → 检验步骤 → 主次操作 → 隐私说明”的高对比结构。
- 全局导航颜色切换到语义令牌，确保后续页面不再从导航栏继承旧橙色；路由、标题和返回行为保持不变。

#### 范围

- 更新最终原始中性色、语义表面/文字/操作/边框/覆盖层和按钮、表面、照片标注等组件配方。
- 保留风险语义令牌和旧主题兼容层，避免未迁移页面丢失状态区分。
- 迁移首页的正常、开始中和开始失败三种状态；开始失败继续使用平台 `Alert`，不改变错误恢复路径。
- 在 390×844 手机视口和宽屏 Web 中验证标题换行、底部操作、长文本、开始中禁用和历史入口。

#### 非目标

- 不修改后端接口、`startChallenge` 请求、Zustand、导航路由或扫描状态机。
- 不迁移扫描页剩余视觉、单品结果、最终报告和历史列表。
- 不实现家庭化学品人格、分享卡图片或新的数据字段。
- 不在本切片锁死未来品牌色；如后续增加强调色，只能替换或扩展令牌并重新验证对比度。

#### 兼容与回退

- 旧页面继续通过 `colors.ts` 与 `spacing.ts` 读取兼容值；首页不再引用旧主题。
- `colors.ts` 的主色兼容别名映射到黑色主操作，因此旧页面会获得可接受的临时黑白基线，但风险色和业务逻辑不变。
- 回退首页时只需恢复旧 `HomeScreen`，令牌和兼容层不会改变 API 数据或本地挑战记录。

#### 验收

- 首页不出现橙色、彩色装饰、Emoji、普通卡片阴影或页面级十六进制颜色。
- 主按钮高度至少 52，次按钮至少 52，所有触控区域至少 44×44；开始中保留尺寸并提供忙碌状态。
- 390×844 下标题、步骤、主次按钮和隐私说明均可到达且不被安全区遮挡；宽屏内容最大 720px。
- Expo SDK 57 / React Native 0.86 / React 19.2.3 兼容；TypeScript、后端测试、Expo Web 导出和真实浏览器截图通过。

#### 实施结果

- 最终原始中性色、语义颜色和组件配方已落入 `tokens.ts`；`colors.ts` 继续作为未迁移页面的兼容适配器。
- `HomeScreen` 已只使用公共基础组件和令牌，保留原有开始挑战、重复点击保护、历史导航和错误提醒。
- 全局导航背景、标题、返回图标和分隔处理已切换到语义令牌。
- TypeScript、Expo Web 导出（539 个模块）和后端 19 项回归测试通过。
- 320×568、390×844、1280×900 浏览器实测无横向溢出；小屏内部滚动可到达完整主次操作和隐私说明，控制台无警告或错误。
- 本切片未提交或推送 GitHub，继续按照重大里程碑集中更新。

### 10.24 第四条功能垂直切片：扫描链路最终视觉迁移（2026-07-18）

#### 决策

- 本切片覆盖“全景输入 → 原图分析 → 原图圈选区域 → 零区域恢复”，沿用已经批准的用户流程、线框层级、三层令牌和组件边界。
- `ScanScreen` 继续作为流程控制器，保留拍照/相册汇合、模型请求、旁白请求、区域推进和错误恢复；功能视图不新增网络或 Store 职责。
- 全景入口与首页统一为左对齐标题、黑白检验说明、单一主操作和相册次操作；不引入新图片素材或装饰性卡片。
- 分析状态继续使用用户刚提交的原图作为全屏背景；黑色蒙层保护文字，白色扫描框、扫描线和编号承担视觉反馈，删除发光阴影和旧式彩色装饰。
- 区域引导继续使用 `PhotoFrame.annotations` 在原图圈选当前与后续区域；当前区域使用黑色实线反白标签，后续区域使用白色虚线和序号。
- 扫描进度、拍摄优先级和已发现雷点在总览层保持黑白，通过文字、编号、边框和符号表达；风险语义色只留给展开后的错误与风险详情。

#### 范围

- 将 `ScanScreen` 页面背景从旧主题兼容层迁移到语义令牌。
- 精修 `PanoramaInputView`、`MagnifierAnimation`、`NarrationBubble`、`ProgressSummary` 和 `AreaGuideView`。
- 保持 `PanoramaEmptyView` 的原图、换图、重拍和再次调用模型恢复路径。
- 验证 320×568、390×844 和宽屏 Web 中的原图比例、内部滚动、底部操作、长描述和区域标注。

#### 非目标

- 不修改相机权限、图片压缩、API 请求、区域数量、识别草稿、确认产品和报告生成逻辑。
- 不迁移 `IdentificationReviewView`、`ResultView`、最终报告或历史页面。
- 不新增图标库、动画库、图片持久化、人格标签或分享卡。
- 不让黑色幽默进入拍摄说明、风险事实、错误恢复和隐私文案。

#### 兼容与回退

- 扫描状态枚举、Store 和接口类型保持不变，视图文件可逐个回退。
- `colors.ts` 仍服务未迁移页面；`ScanScreen` 迁移后不再依赖旧主题背景。
- 分析动画继续服从系统减少动态效果；Web 保持非原生驱动，避免控制台降级警告。

#### 验收

- 全景入口与首页属于同一黑白视觉系统，拍照和相册入口优先级明确。
- 分析页始终显示本次原图；无图片时使用近黑背景，不显示白色空页。
- 当前和后续区域在原图上可区分，缺少边界框时保留文字降级说明。
- 总览层不使用红、绿、蓝、橙作为主要区分方式；错误恢复仍保留可访问的错误语义。
- TypeScript、Expo Web 导出、后端测试和三档真实浏览器截图全部通过。

#### 实施结果

- `ScanScreen` 已切换到语义页面背景；业务请求、状态机和短生命周期图片状态未修改。
- `PanoramaInputView` 已与首页统一为黑白检验档案结构，拍照、相册和隐私说明行为保持不变。
- `MagnifierAnimation` 已删除放大镜卡通运动和发光阴影，改为用户原图上的白色扫描框、扫描线和核对目标；仍服从减少动态效果。
- `ProgressSummary` 和 `AreaGuideView` 的总览状态已中性化，当前/后续区域继续由实线、虚线、编号和完整文字区分。
- 零区域恢复继续显示原图，并保留换图、重拍和明确标注模型调用的再次分析入口。
- 最终验证结果：TypeScript 通过、Expo Web 导出成功（539 个模块）、后端 19 项测试通过；320×568、390×844、1280×900 浏览器无横向溢出和控制台警告。
- 目标 iOS/Android 真机上的相机预览、安全区和相册回跳留到最终设备 QA；本切片未提交或推送 GitHub。

### 10.25 第五条功能垂直切片：单品结果与最终报告（2026-07-18）

#### 决策

- 本切片覆盖单品结果、风险证据、报告评分、雷点分布、零雷点状态、分享和重新挑战，保留后端评分、报告字段、Share 调用和导航行为。
- 单品结果不再把 `status === "safe"` 表述为绝对“安全”，统一写为“当前规则未发现雷点”；报告中的 `safe_items` 显示为“未命中规则”，避免超出规则引擎能力边界。
- 新增可复用的 `RiskCard` 组合组件，统一单品结果和最终报告中的风险等级、涉及产品、风险事实、处置建议、证据状态和来源链接。
- 报告总览保持完全黑白，通过编号、粗细边框、完整文字和三列统计表达风险，不再使用红绿评分圈、彩色圆点或 Emoji。
- 黑色幽默与自嘲只进入单品结果副标题和报告人格标签；风险描述、处置建议、证据状态、来源、错误和隐私文案保持严肃。
- 人格标签由前端受控函数根据 `score` 与 `total_mines` 选择固定模板，不调用模型、不成为风险判断依据、不写入 Store 或报告数据。

#### 范围

- 新增 `RiskCard`，迁移并替代旧 `MineCard` 的展示职责。
- 迁移 `ResultView`、`ScoreDisplay`、`MineList` 和 `ResultScreen` 到公共基础组件与语义令牌。
- 保留 `ReportGeneratingView`，只在需要时补充与报告档案一致的加载文案。
- 验证高危、中危、低危、未命中规则、资料已核验、资料待复核、无来源、长来源标题、零雷点和报告缺失状态。

#### 非目标

- 不修改后端评分公式、风险规则、证据来源、报告模型、分享文案或数据库。
- 不创建包含原图、产品名称、地址或家庭隐私的分享图片。
- 不迁移历史列表，不增加折叠、筛选、导出 PDF 或证据抓取能力。
- 不用人格标签淡化风险等级，也不把“本轮未命中规则”表达为长期安全结论。

#### 兼容与回退

- `RiskCard` 只接收展示所需的结构化字段，不读取 Store、不计算风险、不打开除证据来源以外的外部地址。
- `ResultView` 与 `ResultScreen` 的事件签名、路由和 Store 读取保持不变，可按文件独立回退。
- 旧 `MineCard` 在新组件完成类型、构建和视觉验证且确认无引用后删除。

#### 验收

- 单品结果明确区分“发现雷点”与“当前规则未发现雷点”，不再出现炸弹、庆祝或安全勾选 Emoji。
- 风险事实、建议、证据和来源按固定顺序展示，证据链接具备按钮语义和明确朗读标签。
- 报告评分、风险分布和雷点详情完全黑白，状态不依赖颜色；人格文案与严肃风险信息视觉分区。
- 零雷点状态明确说明“本轮未命中规则不代表以后可以忽略标签或混用风险”。
- TypeScript、Expo Web 导出、后端测试和 320×568、390×844、1280×900 浏览器验证通过。

#### 实施结果

- 已新增共享 `RiskCard`，单品结果与最终报告复用同一风险事实、建议、证据和来源结构；旧 `MineCard` 确认无引用后删除。
- `ResultView` 已移除绝对“安全”结论、彩色状态、Emoji 和旧触控组件，保留继续区域与查看报告事件。
- `ScoreDisplay` 已改为黑白档案表头、规则评分、雷点/未命中统计和受控人格模板，不重新计算后端分数。
- `MineList` 已使用 H/M/L 编号和完整等级文字代替彩色圆点，并复用 `RiskCard` 展示长证据来源与无来源降级。
- `ResultScreen` 已迁移报告缺失、零雷点、分享和重新检查状态；零雷点明确不是永久安全证明。
- 最终验证结果：TypeScript 通过、Expo Web 导出成功（539 个模块）、后端 19 项测试通过；320×568、390×844、1280×900 浏览器无横向溢出和控制台警告。
- iOS/Android 系统分享、外部证据浏览器回跳和目标真机字体留到最终设备 QA；本切片未提交或推送 GitHub。

### 10.26 第五条切片修正：恢复已确认报告构图（2026-07-18）

#### 偏差结论

- 第五条切片错误地把 `design/visual-polish/monochrome-inspection.png` 当作风格参考，而不是最终视觉验收基线。
- 当前实现虽然满足黑白、真实数据、长文本和可访问性，但擅自改成“大档案卡 → 雷点分布 → 完整详情”的构图，偏离已确认的“分数环 → 结论 → 黑底人格 → 三项统计 → 单条风险摘要 → 底部操作”。
- 本次偏差属于组件组合和页面层级错误，不是令牌色值问题；暂停历史页面迁移，先修正报告页。

#### 修正边界

- `ScoreDisplay` 必须恢复 96px 黑色分数环、右侧检查结论、黑底反白人格卡和连续三项统计。
- 报告首屏只显示最高优先级风险摘要；完整 `RiskCard` 列表通过“查看全部风险详情”显式展开，保留长文本、证据和来源。
- 三项统计固定为“雷点 / 当前未命中 / 已检查”，其中已检查数由 `total_mines + safe_items` 派生，不修改后端。
- 顶部导航右侧显示“已完成”；报告首屏和 390×844 视觉稿保持相同阅读顺序。
- 主操作恢复隐私分享语义。当前版本继续通过系统分享面板分享后端提供的无原图文本摘要，不生成或持久化包含产品名称、原图和家庭地址的图片。
- 次操作恢复“查看全部风险详情”；展开后可收起。重新检查移至详情展开后的安静操作，避免改变已存在的重置能力。

#### 保留能力

- 后端评分、雷点数据、证据来源、Store、导航和系统分享调用不变。
- “未命中规则”不得改写成绝对安全；零雷点仍提供限制说明。
- 详情中的风险事实、处置建议、证据和来源继续使用共享 `RiskCard`。

#### 修正验收

- 390×844 首屏与已确认报告视觉稿在结构、主要比例、黑白反差和操作顺序上一致。
- 320×568 可滚动到首屏操作；1280×900 内容最大 720px。
- 多雷点、零雷点、详情展开、详情收起、长来源、分享和重新检查均可达。
- TypeScript、Expo Web 导出和后端回归测试通过。

#### 修正结果

- `ScoreDisplay` 已恢复 96px 分数环、右侧检查结论、黑底人格卡和“雷点 / 当前未命中 / 已检查”三项连续统计。
- `ResultScreen` 首屏已恢复单条最高优先级风险摘要与底部“生成隐私分享卡 / 查看全部风险详情”操作顺序。
- 完整 `MineList` 和共享 `RiskCard` 改为用户显式展开后显示；详情可收起，重新检查能力保留在展开区。
- 顶部导航已增加“已完成”；隐私说明明确当前通过系统分享摘要，不包含原图、产品名称和家庭地址。
- 390×844 对照截图与确认稿的构图、比例和黑白层级一致；320×568 可滚动，1280×900 内容保持 720px。
- 零雷点状态继续说明“未命中规则不等于永久安全”，并使用相同分数环、人格卡和三项统计。
- 最终验证结果：TypeScript 通过、Expo Web 导出成功（539 个模块）、后端 19 项测试通过，浏览器控制台无警告或错误。

### 10.27 单场景快速检查架构修订（2026-07-19，已确认）

#### 产品与领域边界

- 一个 challenge 只对应一张最终采用的全景图、一个场景标签和一份独立场景报告；厨房、卫生间等其他位置必须由用户主动开始新的 challenge。
- 全景分析只允许返回当前图片中 1～3 个信息价值最高的细拍区域，不再因为高风险区域较多放宽到 4～5 个。
- 完成本图最后一个区域后，有至少 1 条用户确认结果才允许生成“本场景抽查评分”；零确认结果属于证据不足，不得计算或返回 100 分。
- 本场景报告只表示当前全景图的抽查结果，不得扩展为整套住宅已经完成检查。

#### 后端契约

- `PanoramaResult.areas` 的 `max_length` 从 5 收紧为 3；Qwen 提示词、区域去重筛选、Mock Provider 和接口测试统一遵守相同上限。
- `PanoramaResult` 新增 `scene_label`，由视觉模型根据画面可见环境生成简短展示标签，例如“厨房水槽场景”；无法可靠判断时返回“当前场景”。
- `scene_label` 写入 `ChallengeState`，并随 `ReportData`、`ChallengeHistoryItem` 返回移动端；它只用于标题和历史检索展示，不参与区域优先级、风险规则或评分。
- 报告服务增加零证据防线：`scan_results` 为空时返回明确的领域错误，不调用 `build_report`；前端分支判断只负责体验，后端校验负责数据正确性。
- 替换全景图只允许发生在当前 challenge 尚无确认结果时；替换会覆盖旧 `scene_label` 和区域列表，不保留或持久化原图。

#### 前端状态与所有权

- `ScanPageStatus` 新增 `evidence_insufficient`，作为 `ScanScreen` 内的流程状态，不新增导航路由。
- `challengeStore` 新增 `sceneLabel`，并提供语义动作 `reviewAreas()` 与 `replacePanorama()`；不向功能视图暴露任意索引修改能力。
- `reviewAreas()` 把 `currentAreaIndex` 重置为 0 并回到 `guide`，保留当前全景图和区域标记。
- `replacePanorama()` 只在没有确认结果时清空场景标签、区域和当前索引并回到 `panorama`；页面同时清理短生命周期的 `panoramaImageUri`。
- `ScanScreen.handleContinue` 在最后区域执行分支：`scanResults.length > 0` 进入 `finishing`，否则进入 `evidence_insufficient`，零证据路径不得调用报告 API。

#### 组件边界

- 新增 `EvidenceInsufficientView` 功能视图，接收原图、区域、跳过数量以及 `onReviewAreas / onReplacePanorama / onExit`；它只负责展示和发出事件，不读取 store、不导航、不请求接口。
- `AreaGuideView` 继续负责本图 1～3 个区域的引导与输入事件，不理解其他房间或新 challenge。
- `ScoreDisplay` 继续展示分数环、结论和三项统计；受控人格模板选择提取为纯函数 `selectChemicalProfile(score, totalMines)`。
- 新增或抽取 `ChemicalProfileCard` 组合组件，只接收 `label` 与 `note`；不得接收风险对象、计算评分或调用模型。
- `ResultScreen` 负责把 `sceneLabel` 传入报告标题，并把“检查另一个场景”解释为重置前端状态后返回首页，而不是继续当前扫描状态机。
- `HistoryScreen` 只展示后端已持久化的 `scene_label`；旧记录缺少该字段时降级为“历史场景”，不阻止打开报告。

#### 迁移顺序

1. 收紧后端区域上限，增加 `scene_label` 与零证据报告校验，并完成后端模型、服务和 API 测试。
2. 更新移动端 API 类型、`ScanPageStatus` 与 store 语义动作，保持旧历史记录兼容。
3. 实现 `EvidenceInsufficientView` 和 `ScanScreen` 分支，验证全部跳过不会调用报告 API。
4. 更新首页、全景、区域、报告和历史单场景文案；保留黑底人格卡并拆分模板选择与展示。
5. 运行后端测试、TypeScript、Expo Web 构建和 320×568、390×844、1280×900 关键状态截图；最后在手机相机与相册链路验证。

#### 非目标与回退

- 不修改风险规则、评分扣分公式、证据来源、图片隐私策略或相机/相册能力。
- 不增加手动填写场景名的新步骤；模型标签失败时使用通用降级文案，避免拉长流程。
- 不把人格标签写入数据库或后端报告，不让娱乐文案影响风险结论。
- 本次以新增状态、字段默认值和小组件为主；任一前端展示变化可按文件回退，后端新增字段均提供默认值以兼容旧 SQLite 记录。

#### 实施门禁

- 已确认输入为 `用户流程.md` v1.1 和 `手机端页面线框图.md` v1.1；视觉令牌与黑白检验档案方向不变。
- `前端组件图与设计令牌映射.md` v1.1、HTML 组件图和 PNG 预览是本阶段评审产物。
- 2026-07-19 产品侧已确认本节与组件图；先新增《单场景快速检查实施计划》，再按上述五步编码。

#### 实施结果（2026-07-19）

- 后端已把 `PanoramaResult.areas` 收紧到最多 3 项；Qwen 原始候选先做验证、去重和排序，再构造最终结果，避免模型偶发多返回时绕过契约。
- `scene_label` 已由 Qwen/Mock 全景分析产生并保存到 `ChallengeState`，随报告和历史返回；旧记录继续降级为“历史场景”。
- 报告服务已增加零证据领域错误 `REPORT_EVIDENCE_REQUIRED`；已有确认结果后再次上传全景返回 `PANORAMA_LOCKED`。
- 移动端已增加 `evidence_insufficient`、`reviewAreas()`、`replacePanorama()` 和 `EvidenceInsufficientView`；全部跳过只进入恢复状态，不调用报告接口。
- 报告人格模板已拆为纯函数 `selectChemicalProfile` 和展示组件 `ChemicalProfileCard`；评分、风险事实和证据未改变。
- 首页、全景输入、报告、历史和导航已改为单场景文案；“检查另一个场景”会重置当前状态并返回首页。
- 未更换 Expo 57、Camera 或 ImagePicker 依赖，也未改变相机/相册权限模型。
- 验证结果：后端 22 项测试、TypeScript、Expo Web 542 模块导出通过；首页、输入、旧历史降级与报告完成 Web 实际渲染，浏览器控制台无警告或错误。
- 尚待目标 iOS/Android 真机验证相机、相册、全部跳过恢复和生成单场景报告四条路径；本次未提交或推送 GitHub。

### 10.28 异常恢复架构修订（2026-07-19，已确认）

#### 上游与范围

- 本节以上游已确认的 `异常恢复用户流程.md` v1.2 和 `异常恢复页面线框图.md` v1.2 为准，覆盖首页创建、全景请求、无效全景、细拍识别、产品确认和报告生成六组失败状态。
- 可恢复错误必须留在当前任务上下文内，不再使用系统 `Alert` 覆盖页面；权限提示、破坏性退出确认和系统设置跳转仍可使用原生对话框。
- 本节只调整移动端错误建模、状态所有权、组件边界和迁移顺序，不修改风险判断、评分、模型提示词、证据或照片隐私边界。

#### 错误契约与展示分层

- `ApiError` 只保存协议事实：HTTP 状态、稳定错误码、`requestId` 和适用时的 `retryAfterSeconds`；禁止把追踪号、API 地址、端口或开发建议拼进用户消息。
- 新增纯函数 `toAppFailure(error)`，把传输层错误归一化为 `network / timeout / rate_limited / invalid_photo / challenge_expired / service / unknown`。
- 新增纯函数 `getRecoveryCopy(operation, failure)`，根据 `start / panorama / detail / confirm / report` 生成用户标题、解释和动作文案；展示层不直接解释后端错误码。
- `IMAGE_TOO_LARGE`、`UNSUPPORTED_IMAGE_TYPE` 和等价图片校验错误归为 `invalid_photo`；网络、超时和临时服务异常保持可重试；`CHALLENGE_NOT_FOUND`、无效识别草稿等不可继续错误引导重新开始。
- 后端继续返回统一错误信封和 `X-Request-ID`；本轮不改变现有错误响应结构，只补齐前端映射测试。若后续新增稳定错误码，必须先更新本节映射与测试。

#### 状态与照片生命周期

- `challengeStore` 继续只保存挑战 ID、场景、区域、已确认结果和报告，不保存错误对象、失败照片 URI、重试闭包或页面请求状态。
- `HomeScreen` 局部持有创建失败；`ScanScreen` 使用判别联合 `ScanRecoveryState` 局部持有失败操作、`AppFailure` 和当前重试所需的短期照片/区域引用。
- 全景请求失败保留当前 `panoramaImageUri`；细拍识别失败同时保留全景定位和 `pendingDetailImageUri`；确认失败保留识别草稿与用户编辑字段；报告失败保留 store 中的已确认结果。
- 当前版本仍不跨应用重启恢复照片。失败照片只存在于页面会话和上传请求内，不写入 SQLite、文件系统、日志或长期缓存。
- 每种操作只能有一个请求进行中；重试复用原 `challengeId`、照片 URI 或草稿，不重新创建 challenge，也不重复识别已经确认的结果。

#### 组件边界

- 新增组合组件 `RecoveryPanel`，统一错误符号、标题、说明、短追踪号、主次动作、最小触控尺寸和 live-region 播报；它不读取错误码、不调用 API、不导航。
- 新增 `PanoramaRecoveryView`，展示保留的全景图和恢复动作；`retry_request` 以重试原图为主操作，`replace_invalid_photo` 以重拍/换图为主操作。
- 新增 `DetailRecoveryView`，同时展示全景区域定位和失败细拍，发出 `onRetrySamePhoto / onRetake / onSelectFromAlbum / onSkip`。
- `IdentificationReviewView` 增加确认失败恢复槽位，继续在组件本地保留表单值；失败不得重新挂载整个表单或重新请求识别。
- 新增 `ReportRecoveryView`，展示已确认结果摘要并发出 `onRetry / onReviewConfirmedResults`；页面重试只重新请求报告。
- 功能视图只接收归一化失败数据与 typed callbacks，不接收 `api`、Zustand store、navigation 或原始异常。

#### 页面控制器

- `HomeScreen` 负责开始请求、请求互斥、创建失败恢复和成功导航。
- `ScanScreen` 继续作为扫描流程控制器，负责操作到恢复状态的映射、请求互斥、照片短期引用、API 调用、store 更新与页面变体选择。
- 不在本轮预先提取 `useScanController`。完成全景代表性切片后，只有当恢复分支仍导致控制器难以单测或职责重复时，才另行更新架构并提取 hook。
- 任何恢复视图都不得自行决定“重试是否安全”；页面根据 `operation + failure.kind` 传入唯一主操作和可选次操作。

#### 令牌与可访问性

- 异常恢复继续使用既有三层令牌，只新增 `recoveryPanel.*` 组件配方并映射到 `surface.* / text.* / border.* / status.error / action.*`。
- 错误不得只靠颜色表达，必须同时提供符号、标题、解释和恢复动作；所有动作触控目标不小于 44×44。
- 新错误出现时由 `RecoveryPanel` 播报一次；追踪号不应被重复朗读。主按钮可访问名称必须同时包含动作和对象，例如“重试分析这张全景照片”。
- 长中文、系统字体放大和底部动作不得被裁切；页面允许纵向滚动，不用绝对定位把操作压在内容上。

#### 小切片迁移与回退

1. 增加 `AppFailure`、错误码映射、用户文案纯函数和单元测试，保留旧 `ApiError` 调用接口的兼容层。
2. 实现 `RecoveryPanel` 和组件令牌，完成动作层级、追踪号、长文本和播报测试。
3. 以“首页创建失败 + 全景请求失败 + 无效全景”为代表性切片，移除对应系统 `Alert`，验证保留并重试原图。
4. 实现细拍与确认恢复，验证全景定位、失败细拍、草稿和用户编辑不丢失。
5. 实现报告恢复，失败留在报告阶段，只重新请求报告。
6. 清理对应旧错误字符串和 `Alert`，运行 TypeScript、Expo、Android 真机、异常注入、可访问性和关键截图回归。

- 每个切片独立替换一个错误入口；未迁移入口继续使用旧实现，避免一次性重写。
- 如果新恢复视图出现阻断问题，可按入口回退为旧页面分支；错误信封、challenge 数据和后端持久化均不受影响。
- 组件图、HTML 预览和 PNG 经产品确认后，才新增异常恢复实施计划并修改应用代码。

#### 实施门禁确认

- 2026-07-19 产品侧明确确认 `异常恢复用户流程.md`、`异常恢复页面线框图.md` 和异常恢复 v1.2 组件边界。
- Stage 1～4 均已通过；允许先新增第一条异常恢复实施计划，再迁移错误契约、`RecoveryPanel`、首页和全景代表性切片。

#### 第一条功能切片实施结果（2026-07-19）

- `ApiError` 已保留结构化状态、错误码、追踪号和 `Retry-After`；面向用户的消息不再拼接 API 地址、端口或完整追踪号。
- 新增 `AppFailure`、`toAppFailure()`、`getRecoveryCopy()` 及 6 项纯函数测试；通用参数校验不会被误判为无效照片。
- 新增共享 `RecoveryPanel` 与 `recoveryPanel.*` 组件令牌；组件只负责错误语义、短追踪号、动作层级和可访问性，不调用 API、store 或导航。
- 首页创建失败已从系统 `Alert` 迁移为就地恢复，首页说明和历史报告入口继续可用。
- 全景分析失败已由 `PanoramaRecoveryView` 持有短期照片 URI；网络/超时以重试同一张为主操作，无效图片以重拍或换图为主操作，失效 challenge 返回首页重新开始。
- 重试继续复用原 `challengeId` 与原照片 URI；请求期间切换到分析状态，避免恢复按钮产生并发重复请求。
- TypeScript、Expo Web 551 模块导出、后端 22 项测试和首页断网真实渲染均通过。
- Android 真机仍需分别用相机和相册验证全景失败状态、照片保留、重试同一张、重拍与换图；该项通过前不迁移细拍、确认和报告恢复。
- 本切片没有修改风险规则、评分、AI 提示词、证据、照片持久化策略或 GitHub 仓库。
