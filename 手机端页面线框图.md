# 家庭化学品排雷大挑战——手机端页面线框图

> 版本：v1.1（单场景快速检查，已确认）
>
> 日期：2026-07-19
>
> 上游输入：[用户流程](./用户流程.md)  
> 可视化画板：[打开九屏低保真线框图](./design/wireframes/mobile-low-fi.html)

![手机端单场景快速检查低保真线框图](./design/wireframes/mobile-low-fi.png)

## 一、本阶段解决什么

本线框图只确定：

- 每个页面首先展示什么信息。
- 当前页面唯一的主操作是什么。
- 拍照、相册、重试、跳过和退出如何排列。
- 图片、进度、风险说明和恢复入口是否始终可见。
- 九组页面和“全部跳过”恢复变体之间是否能完整承接已确认的单场景用户流程。

本阶段不确定：

- 最终品牌色、渐变、插画或图标风格。
- 最终字号、字体和动效参数。
- React Native 组件如何拆分。
- 桌面 Web 的宽屏排版。

## 二、全局布局规则

### 手机基准

- 基准画板：390×844。
- 页面从上到下分为：系统安全区、导航/进度区、主要内容区、底部操作区。
- 主要触控目标不小于 44×44。
- 页面默认单列纵向阅读，不依赖横向卡片承载关键信息。

### 动作优先级

| 级别 | 用途 | 示例 |
|---|---|---|
| 主操作 | 推进当前核心任务，每页最多一个 | 开始场景检查、拍摄本图当前区域、确认并评估、生成本场景评分 |
| 次操作 | 等价输入或可恢复动作 | 从相册选择、重新拍照 |
| 文字操作 | 低频、可跳过或可能产生额外成本 | 跳过区域、再次分析原图、再排一次 |

### 扫描上下文

- 全景分析、零区域和区域引导必须显示同一张全景照片。
- 一张全景图建立一次场景检查边界，只产生 1～3 个细拍任务和一份独立报告。
- 单品分析与识别确认必须显示本次单品照片。
- 进度在区域引导、单品结果中持续可见。
- 完成本图后流程直接进入本场景报告，不自动进入厨房、卫生间等其他场景。
- 安全提示不能只用颜色、图标或 Emoji，必须有明确文字。

## 三、九组页面说明

### 01 首页

目标：让第一次使用的用户在数秒内理解“一次只检查一个场景”并开始。

信息顺序：

1. 产品类别和核心标题。
2. 一句话价值说明。
3. 品牌视觉占位。
4. “开始场景检查”主按钮。
5. “查看历史报告”次按钮。
6. 三步玩法摘要，明确“一张全景、最多三处近景、一份场景报告”。

决策：历史入口保留，但不与开始按钮争夺主视觉。

### 02 全景输入

目标：在打开相机前讲清楚拍摄对象、合格照片标准和单场景边界。

信息顺序：

1. 顶部退出、标题和步骤进度。
2. “一次只拍一个场景”的任务标题与单个场景示例。
3. 正确拍摄范围占位图。
4. 图片隐私说明。
5. 拍照主按钮。
6. 相册次按钮。

状态变体：无相机权限时保留任务说明，提供再次授权、相册和取消。

范围约束：厨房、卫生间、储物柜只作为三个可选示例，不得在同一次任务里并列成待完成清单。

### 03 分析

目标：明确系统正在分析哪张照片，以及完成后会得到什么。

信息顺序：

1. 原照片和扫描反馈。
2. 当前阶段说明。
3. 三步处理进度。
4. 返回并保留照片的低优先级入口。

错误变体：保留原图，出现“重试分析”和“换一张”，不直接回到空白输入页。

### 04 零区域

目标：把“没有找到区域”呈现为可恢复结果，不表现成系统崩溃。

操作顺序：

1. 从相册换一张。
2. 重新拍照。
3. 再次分析原图，并明确会再次调用模型。

决策：原图始终保留；再次分析不能作为默认主操作，避免无意识重复消耗额度。

### 05 区域引导

目标：用户无需阅读长描述，也能从原图看懂下一处要拍哪里。

信息顺序：

1. 本场景区域进度和已发现雷点数量；总数只能是 1～3。
2. 全景图：当前区域实线强调，后续区域虚线弱化。
3. 当前区域名称与拍摄要求。
4. 拍摄主按钮。
5. 相册次按钮。
6. 跳过文字操作。

退出变体：点击退出时弹出“继续排雷 / 退出挑战”，明确未完成记录不会生成报告。

### 05B 全部跳过

目标：当用户跳过本图全部区域时，阻止系统把“没有检查证据”显示为 100 分。

信息顺序：

1. “证据不足，暂时无法评分”的明确结论。
2. 保留原全景图与 1～3 个区域标记。
3. 说明已跳过数量以及为什么不能评分。
4. “返回原图补拍”主按钮。
5. “换一张全景图”和“退出本次检查”次级操作。

决策：该状态不是报告页，不显示分数、风险等级或“未发现雷点”。

### 06 识别确认

目标：让用户先确认模型观察到的产品事实，再进入安全判断。

信息顺序：

1. 本次单品照片。
2. 置信度与核对提示。
3. 品牌、产品名、品类、成分字段。
4. “确认信息并评估风险”主按钮。
5. 重新拍照与相册换图。
6. 本地规则边界说明。

校验：产品名和品类必填；确认失败后必须保留用户编辑内容。

### 07 单品结果

目标：先给用户可理解的结论，再解释原因、行动和证据。

信息顺序：

1. “需要注意”或“当前规则未发现雷点”的文字结论。
2. 已由用户确认的产品事实。
3. 风险标题、等级、原因与建议。
4. 证据状态和来源入口。
5. 识别/规则不等于实验室检测的边界说明。
6. “继续检查本图下一处”或“生成本场景评分”的主按钮。

文案约束：不再把无规则命中直接写成绝对“安全”。

### 08 报告

目标：让用户先看当前全景图对应场景的抽查情况，再按需阅读风险详情。

信息顺序：

1. “本场景抽查评分”和概括性结论。
2. 黑底反白的受控人格标签，例如有雷点时“家庭炼金术预备役”，零雷点时“本轮没翻车型”。
3. 雷点、当前未命中、已检查数量。
4. 风险产品摘要列表。
5. 证据状态和详情入口。
6. 分享主按钮。
7. “检查另一个场景”文字操作，返回首页后由用户主动创建新 challenge。

失败变体：报告生成失败时回到已完成扫描结果，主按钮明确写“重新生成报告”。

边界说明：报告不得暗示整套住宅已经检查完成。

人格标签边界：标签由分数和雷点数选择受控模板，只负责黑色幽默与自嘲表达；不得参与风险计算，不得替代风险事实，也不得出现在错误、隐私或处置建议中。

### 09 历史

目标：快速打开已完成报告，并清楚理解未完成记录的限制。

记录状态：

- 已完成：显示场景记录、日期、分数、雷点，可打开报告；一条记录对应一张全景图。
- 未完成：不可点击，解释当前版本不能继续，也没有报告。
- 空数据：说明完成挑战后会出现记录，并提供开始挑战入口。
- 加载失败：保留历史页并提供重新加载。

## 四、跨页面一致性检查

| 检查项 | 线框图处理 |
|---|---|
| 当前任务是否明确 | 每页标题只描述当前任务 |
| 主按钮是否唯一 | 每组页面最多一个深色主按钮 |
| 单场景边界是否明确 | 首页、全景输入、区域进度和报告都说明当前检查只对应一张全景图 |
| 任务长度是否可控 | 每张全景图只显示 1～3 个细拍区域 |
| 相册是否可用 | 所有图片输入节点均保留相册入口 |
| 原图是否保留 | 分析、零区域、引导、确认均显示对应照片 |
| 失败是否可恢复 | 失败作为当前页面状态变体处理 |
| 进度是否连续 | 顶部步骤和区域计数持续显示 |
| 全部跳过是否误评分 | 进入证据不足恢复状态，不生成报告 |
| 风险表达是否克制 | 文字结论包含规则与检测边界 |
| 退出后果是否明确 | 未完成挑战使用二次确认 |

## 五、视觉验收

- [x] 可编辑 HTML 画板包含全部 9 组页面和“全部跳过”恢复变体。
- [x] 受影响的 6 组状态已渲染到 PNG 总览：图片为 1280×500，手机框内部保持 390×844 基准比例。
- [x] 逐屏检查 `.screen` 的可见高度与内容高度，10 个手机框均无裁切或溢出。
- [x] “证据不足”长文案、三层操作和本场景报告边界说明均在手机框内可见。
- [x] 报告加入黑底人格标签后，评分、三项统计、风险摘要和底部操作仍无裁切。
- [x] 首页、全景输入、区域引导、全部跳过、报告和历史的单场景语义保持一致。
- [x] 灰度画板只表达结构和动作优先级，没有改动已确定的黑白视觉方向。

## 六、需要确认的线框决策

- [x] 首页主操作改为“开始场景检查”，并在三步摘要中说明一图一报告。
- [x] 全景输入只给单个场景拍摄示例，不把多个房间表现成待办清单。
- [x] 区域进度只显示 1～3 个细拍任务，并使用“本图下一处”。
- [x] 全部跳过时进入“证据不足”恢复页，不进入报告。
- [x] 报告标题和分数明确限定为“本场景抽查评分”。
- [x] 报告保留黑底人格标签，并与严肃风险事实明确分区。
- [x] “检查另一个场景”返回首页，新场景不串联在当前检查之后。
- [x] 拍照与相册在所有图片输入节点保持并列。
- [x] 分析失败后保留照片，并提供原地重试。
- [x] 单品结果继续使用“当前规则未发现雷点”，不表达绝对安全。
- [x] 未完成历史记录只解释状态，暂不提供删除或继续功能。

> 2026-07-15：产品方已确认以上线框决策，同意进入移动端视觉规范阶段。
>
> 2026-07-19：产品方确认单场景快速检查线框图，同意修订组件边界与架构方案。

确认以上结构后，由于视觉规范已经存在，下一步不重做配色，而是修订组件边界与迁移顺序，再更新架构方案并进入功能实现。
