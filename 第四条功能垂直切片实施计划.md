# 家庭化学品排雷大挑战——第四条功能垂直切片实施计划

> 版本：v1.0
> 日期：2026-07-18
> 已批准输入：[用户流程](./用户流程.md) · [手机端页面线框图](./手机端页面线框图.md) · [移动端视觉规范](./移动端视觉规范.md) · [前端组件图与设计令牌映射](./前端组件图与设计令牌映射.md)

## 范围

- 将黑白检验档案视觉迁移到全景输入、原图分析、区域引导和零区域恢复。
- 保留用户原图作为分析背景和区域圈选底图。
- 统一扫描进度、优先级、标注、旁白和输入操作的黑白组件配方。
- 让 `ScanScreen` 页面背景脱离旧主题兼容层。

## 非目标

- 不修改 API、Zustand、路由、区域数量、图片压缩和扫描状态机。
- 不迁移识别确认、单品结果、最终报告和历史页面。
- 不增加新依赖、图标库、图片素材、人格数据或分享功能。
- 不改变相机权限、相册、重试、跳过和错误恢复行为。

## 架构变化

- `ScanScreen` 继续拥有网络与状态编排，扫描视图保持纯展示和语义事件输出。
- `PhotoFrame` 继续是原图与归一化标注的唯一组合组件。
- 扫描总览只使用中性状态标签；错误和展开后的风险详情仍可使用语义状态色。
- 分析动画只引用语义令牌和组件配方，不添加页面级颜色或阴影。

## 迁移顺序

1. 迁移扫描页背景和全景入口。
2. 精简原图分析动画与旁白样式。
3. 中性化扫描进度和拍摄优先级。
4. 校验区域实线/虚线标注和无框降级。
5. 验证零区域恢复、长文本和多尺寸滚动。
6. 清理扫描链路的旧主题引用与装饰阴影。

## 切片检查表

| 切片 | 文件 | 测试 | 功能验收 | 状态 |
|---|---|---|---|---|
| 扫描入口 | `ScanScreen`、`PanoramaInputView` | TypeScript、截图 | 拍照/相册行为不变 | 已完成 |
| 原图分析 | `MagnifierAnimation`、`NarrationBubble` | 减少动态、截图 | 原图背景和加载反馈保留 | 已完成 |
| 区域引导 | `ProgressSummary`、`AreaGuideView`、`PhotoFrame` | 长文本、标注 | 当前/后续区域可区分 | 已完成 |
| 零区域恢复 | `PanoramaEmptyView` | 恢复路径 | 换图/重拍/再次分析可达 | 已完成 |

## 可访问性检查

- 拍照、相册和恢复按钮不低于 44×44，主要按钮保持 52px。
- 分析容器暴露进度语义和正在处理的完整文字。
- 当前区域、后续区域、优先级和雷点数量均有文字说明，不依赖颜色。
- 动画服从系统减少动态效果；背景照片上的文字有实底或强蒙层。

## 响应式与平台检查

- 320×568 可纵向滚动到所有操作，不发生横向溢出。
- 390×844 中原图、说明和主次操作均可到达。
- 宽屏内容最大 720px，照片最大 520px，分析背景不拉伸原图。
- iOS、Android 与 Web 不增加平台专属依赖。

## 回退策略

- 每个功能视图可独立回退，不影响 API、Store 或挑战记录。
- 旧主题兼容层继续存在，未迁移页面不会因本切片删除导出。
- 原图 URI 仍只保存在 `ScanScreen` 的短生命周期状态中。

## 功能完成标准

- 扫描入口、分析和区域引导与首页属于同一黑白视觉系统。
- 分析页显示用户本次照片，区域页在同一张原图上圈选下一处拍摄位置。
- 拍照、相册、零区域恢复、跳过和错误恢复行为不变。
- TypeScript、后端测试、Expo Web 导出及三档浏览器视觉检查通过。

## 交接到后续页面迁移

- 真实页面/状态清单：全景输入、分析中、零区域、区域引导、区域错误。
- 仍在使用兼容令牌：识别确认、单品结果、最终报告、历史。
- 已知视觉不一致：识别与报告仍存在彩色状态表面和旧页面结构。
- 后续切片：单品结果与最终报告的黑白风险档案迁移。

## 实施与验证结果

- 扫描入口已与首页统一为左对齐标题、黑白检验说明、拍照主操作和相册次操作。
- 分析页继续使用用户本次照片作为背景；扫描框、扫描线、核对目标和旁白全部使用语义令牌，删除发光阴影。
- 区域页在同一张原图上使用黑色实线标识当前区域、白色虚线标识后续区域，并增加明确文字图例。
- 拍摄优先级和已发现雷点在总览层改为中性黑白标签；错误恢复仍保留完整错误语义。
- TypeScript、Expo Web 导出（539 个模块）和后端 19 项回归测试通过。
- 320×568、390×844、1280×900 浏览器实测无横向溢出或控制台警告；小屏内部滚动可到达完整操作。
- 零区域状态的换图、重拍和再次分析按钮高度分别为 52、52、44px。
- 当前剩余风险是真机相机画面、安全区和系统相册回跳仍需在目标 iOS/Android 设备做最终 QA。
- 本切片未提交或推送 GitHub，继续按照重大里程碑集中更新。
