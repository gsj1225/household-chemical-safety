# 家庭化学品排雷大挑战——单场景快速检查实施计划

> 版本：v1.0
> 日期：2026-07-19
> 已确认输入：`用户流程.md` v1.1、`手机端页面线框图.md` v1.1、`前端组件图与设计令牌映射.md` v1.1、`架构方案.md` 10.27
> 视觉约束：沿用现有黑白检验档案方向和语义令牌，本阶段不重新决定最终配色

## 范围

- 把一次 challenge 收敛为“一张全景图、一个场景、1～3 个细拍区域、一份场景报告”。
- 后端增加 `scene_label`，统一全景区域上限，并拒绝零确认结果评分。
- 移动端增加“证据不足”恢复状态，让用户可以重看区域、换全景图或退出。
- 场景报告和历史记录展示场景标签，报告保留黑色幽默人格卡。
- 把人格选择逻辑和人格卡展示拆开，降低后续文案与视觉调整的耦合。

## 非目标

- 不修改风险识别规则、评分公式、雷点证据和模型供应商。
- 不增加手动填写场景名的步骤，不持久化原始照片。
- 不重做已经稳定的相机、相册、识别复核和分享能力。
- 不在本阶段决定最终色板、插画、图标、阴影、圆角细节或动效。
- 不在本次普通更新中提交或推送 GitHub；待形成重大稳定版本再统一更新。

## 架构变化

- `PanoramaResult`、`ChallengeState`、`ReportData` 和 `ChallengeHistoryItem` 增加兼容旧数据的场景标签字段。
- Qwen 与 Mock 全景分析均只返回 1～3 个区域；后端模型再次校验上限。
- 报告服务在调用报告构建器前检查 `scan_results`，空证据返回领域错误。
- `challengeStore` 持有 `sceneLabel`，只暴露 `reviewAreas()` 和 `replacePanorama()` 两个恢复动作。
- `EvidenceInsufficientView` 是 `ScanScreen` 内部状态视图，不新增导航路由，也不直接访问 store 或 API。
- `selectChemicalProfile` 是纯函数；`ChemicalProfileCard` 只展示受控标签和说明。

## 迁移顺序

1. 收紧后端区域契约并增加场景标签与零证据校验。
2. 更新移动端类型、状态枚举和 store 语义动作。
3. 接入证据不足恢复视图与 `ScanScreen` 最后区域分支。
4. 更新单场景文案、报告人格组件和历史场景展示。
5. 运行自动化检查并对关键状态做手机、窄屏和桌面 Web 回归。

## 小切片清单

| 切片 | 文件 | 测试 | 功能验收 | 状态 |
|---|---|---|---|---|
| 后端契约 | `backend/app/models/*`、AI Provider | 模型与 Qwen 测试 | 全景最多 3 区域并返回场景标签 | 已完成 |
| 后端报告防线 | report/challenge service 与 API | 流程测试 | 零确认结果不能生成 100 分报告 | 已完成 |
| 移动端状态 | types、store | TypeScript | 场景标签和恢复动作可预测 | 已完成 |
| 证据不足恢复 | `ScanScreen`、`EvidenceInsufficientView` | 分支检查、TypeScript | 全部跳过不会请求报告，可回看或换图 | 已完成 |
| 场景报告 | Result、History、人格组件 | TypeScript 与浏览器截图 | 场景标题、黑色幽默标签和旧记录降级正常 | 已完成 |
| 全链路验证 | backend/mobile | pytest、tsc、Expo Web | 自动化与 Web 已通过，真机相机/相册待验 | 设备验收待办 |

## 无障碍检查

- 所有新增按钮提供明确可读标签，点击区域不小于现有按钮规范。
- 证据不足不能只靠颜色表达，必须同时有标题、正文和动作说明。
- 图片区域标记提供编号和文本列表，读屏用户不依赖框线位置理解。
- 动态状态切换后保持合理的标题层级，不制造只有图标的关键操作。

## 响应式与平台检查

- 320×568：页面允许纵向滚动，主要动作不被遮挡。
- 390×844：与已确认线框图的信息顺序一致。
- 1280×900 Web：内容宽度保持移动端阅读尺度，不横向拉伸。
- iOS/Android：继续使用 Expo 57 现有 Camera 和 ImagePicker 能力，不更换依赖或权限模型。

## 回退策略

- 后端新增字段均有默认或降级值，旧 SQLite challenge 和历史记录仍可读取。
- 新状态只由最后区域且零确认结果触发；出现问题可回退为原 `guide` 状态，不影响已确认结果。
- `ChemicalProfileCard` 与选择器拆分不改变评分和风险事实，可按组件文件独立回退。
- 不执行数据库破坏性迁移，不删除用户现有数据。

## 功能完成标准

- 一张全景图最多产生 3 个近景区域，完成后不会自动要求厨房之外的其他房间。
- 至少确认 1 个近景结果才能生成本场景报告；全部跳过时只进入恢复页。
- 报告与历史均展示场景标签，旧记录显示“历史场景”。
- 报告继续展示黑色幽默人格卡，但标签不参与识别、评分或持久化。
- 后端测试、TypeScript 检查和 Expo Web 构建通过；关键控制台无新增错误。

## 实施验证结果

- [x] 后端 22 项测试通过，覆盖最多 3 区域、场景标签、零证据拒绝和确认后禁止换全景。
- [x] TypeScript `--noEmit` 检查通过。
- [x] Expo 57 Web 导出通过，共 542 个模块。
- [x] 1280×720 实际渲染检查：首页、单场景输入、历史旧数据降级和场景报告均正常。
- [x] 报告继续显示黑底人格卡、“本轮没翻车型”和“检查另一个场景”。
- [x] 浏览器控制台无新增警告或错误。
- [ ] 目标 iOS/Android 真机分别验证相机与相册入口、全部跳过恢复和至少确认一个区域后的报告。

## 交接到最终视觉打磨

- 真实页面/状态清单：全景输入、区域引导、证据不足、识别复核、场景报告、历史列表。
- 暂用令牌：继续使用现有黑白语义令牌，不新增页面私有色值。
- 已知视觉差异：最终色板、阴影、圆角和动效尚未定稿。
- 最终视觉待定项：报告风险强调色、场景标签层级、区域标记强调度和人格卡细节。
