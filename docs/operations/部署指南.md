# 排雷挑战 - 部署指南

## 开发环境（当前）

### 后端
```bash
cd backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 移动端
```bash
cd mobile
cp .env.example .env
# 将 EXPO_PUBLIC_API_BASE_URL 改为电脑局域网 IP 或后端公网地址
npx expo start
```

### 电脑浏览器预览

先启动后端，再运行：

```bash
cd mobile
npm run web
```

浏览器打开 `http://localhost:8081`。进入相机页后可授权电脑摄像头，或点击“电脑演示：使用示例照片”走通 Mock 流程。
手机安装 Expo Go App，扫描二维码即可预览。

### 手机连接后端的配置

手机通过 Expo 连接时，需要能访问后端 API。两种方案：

**方案A：同一局域网（推荐开发用）**
1. 查看电脑局域网IP：`ipconfig` → 找到 IPv4 地址（如 192.168.1.100）
2. 修改 `mobile/.env` 中的地址：
   ```typescript
   EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:8000/api
   ```
3. 确保手机和电脑在同一WiFi下

**方案B：内网穿透（远程演示用）**
1. 使用 ngrok：`ngrok http 8000`
2. 获得公网地址如 `https://xxx.ngrok-free.app`
3. 修改 `mobile/.env`：
   ```typescript
   EXPO_PUBLIC_API_BASE_URL=https://xxx.ngrok-free.app/api
   ```

---

## 生产部署（比赛Demo）

### 后端部署到云服务器

**方式1：Docker部署**
```bash
# 在服务器上
cd backend
docker build -t mine-challenge-api .
docker run -d -p 8000:8000 --env-file .env \
  -v mine-challenge-data:/app/data --name api mine-challenge-api
```

**方式2：直接运行**
```bash
cd backend
pip install -r requirements.txt
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 &
```

### Android APK 打包

**前置条件：**
- 注册 Expo 账号：https://expo.dev
- 安装 EAS CLI：`npm install -g eas-cli`
- 登录：`eas login`

**打包命令：**
```bash
cd mobile
eas build -p android --profile preview
```

打包完成后，Expo 会提供一个 APK 下载链接，安装到 Android 手机即可。

**修改API地址：**
打包前务必修改 `mobile/src/services/api.ts` 中的 `API_BASE` 为后端公网地址。

### AI 接入

Qwen API 已完成代码接入，部署时执行：
1. 在 `.env` 中设置：
   ```
   AI_PROVIDER=qwen
   QWEN_API_KEY=<deployment-secret>
   QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
   QWEN_VL_MODEL=qwen3-vl-plus
   QWEN_TEXT_MODEL=qwen-flash
   ```
2. 确认 API Key 与服务区域匹配，并完成一次真实图片联调
3. 重启后端即可，移动端无需改动

---

## 环境检查清单

- [ ] 后端能启动且 /health 返回 200
- [ ] 手机能访问后端 API（用手机浏览器访问 /health 验证）
- [ ] Expo Go 能加载移动端
- [ ] 拍照功能正常（相机权限已授权）
- [ ] 完整流程走通：开始→全景→细拍→结果→报告
- [ ] APK 能安装运行
- [ ] AI 接口已接入（比赛前）
